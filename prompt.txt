# FieldLine — PASS 4: Schedule Importer

You are implementing PASS 4 — Schedule Importer.

FieldLine repository:

itshim998/FieldLine

Local directory:

C:\Users\Sounak\Desktop\Flagship AI projects\FieldLine

PASS 0, PASS 1, PASS 2, and PASS 3 are complete.

Before coding, inspect the current repository carefully. Do not assume the earlier architecture is still exactly as originally described.

---

# CURRENT REPOSITORY STATE

PASS 2 already provides the database foundation.

The existing SQLite schema already contains:

- projects
- schedules
- activities
- progress_updates
- evidence
- activity_matches
- activity_progress
- project_events

The existing domain types already contain:

- Schedule
- CreateScheduleInput
- Activity
- CreateActivityInput

The existing project lifecycle already allows a user to:

- create a project;
- select/open a project;
- remain in a current project context.

Build the importer on top of those existing foundations.

Do NOT recreate the schema.

Do NOT duplicate the project architecture.

---

# OBJECTIVE

Import real schedule data into the currently selected FieldLine project.

Initial supported formats:

- CSV
- XLSX

The core pipeline is:

```text
Uploaded file
    ↓
file/type/size validation
    ↓
parser
    ↓
column/header mapping
    ↓
canonical import rows
    ↓
basic structural checks
    ↓
transaction
    ↓
schedule record
    ↓
activity records
    ↓
SQLite
EXIT CRITERION

A real CSV or XLSX schedule file can be uploaded from an open project and produce usable persisted FieldLine activities that can be visibly inspected in the UI.

CRITICAL PASS BOUNDARY

Do NOT implement PASS 5 or PASS 6.

PASS 4 is the importer.

Do not build:

deep date normalization;
quantity normalization;
unit normalization;
aggressive text normalization;
advanced duplicate-resolution logic;
comprehensive schedule validation;
activity semantic validation;
schedule quality scoring;
AI schedule interpretation.

Those belong to later passes.

The importer may perform only the minimum structural checks necessary to prevent obviously unusable data from reaching SQLite.

FIRST: INSPECT THE CURRENT REPOSITORY

Before modifying anything, inspect:

current project router/service;
current project workspace UI;
existing database layer;
schedule/activity schema;
domain types;
repository conventions;
validation conventions;
error handling;
setup/reset behavior;
tests;
package.json.

Determine exactly where the new importer should fit.

Preserve existing conventions.

ARCHITECTURE

The importer must follow:

React UI
    ↓
HTTP upload route
    ↓
file validation
    ↓
Import service
    ↓
parser adapter
    ↓
canonical import rows
    ↓
Schedule/Activity repositories
    ↓
SQLite

Do NOT put:

CSV parsing in routes;
XLSX parsing in routes;
SQL in services;
SQL in parsers;
database calls in the frontend.
FILE UPLOAD

Add local multipart upload support.

Use a minimal library such as:

multer

Do not introduce cloud storage.

Uploaded files must remain local.

Prefer a temporary upload location and clean up temporary files after the import completes or fails.

Do not expose arbitrary filesystem paths to the browser.

SUPPORTED FILES

Support:

.csv
.xlsx

Reject unsupported formats cleanly.

Also enforce a reasonable local file-size limit so a malformed huge upload cannot accidentally consume excessive memory.

Do not invent a complicated document-management system.

PARSERS

Create separate parser boundaries.

Conceptually:

ScheduleParser
├── CsvScheduleParser
└── XlsxScheduleParser

Each parser should produce the same canonical import representation.

Do not let downstream services care whether the source was CSV or XLSX.

For example, conceptually:

CanonicalScheduleImportRow

externalId
name
description
wbsCode
location
start
finish
quantity
unit

Adapt naming to the existing repository conventions.

HEADER MAPPING

Real schedule files will not necessarily use exactly one canonical header spelling.

Support reasonable aliases for the core columns.

At minimum recognize variants of:

Activity ID
Activity Name
Description
WBS
Location
Start
Finish
Quantity
Unit

Examples of reasonable aliases include:

Activity ID
ActivityID
ID
Task ID
Activity

Activity Name
ActivityName
Task Name
Name

WBS
WBS Code
WBSCode

Start
Start Date
Planned Start
PlannedStart

Finish
Finish Date
Planned Finish
PlannedFinish

Quantity
Qty
Planned Quantity

Unit
UOM
Units

Do not build a massive synonym engine.

Keep aliases explicit and maintainable.

Header matching can be case-insensitive and whitespace-tolerant.

REQUIRED VS OPTIONAL COLUMNS

At minimum, the importer needs enough data to create an activity safely.

Required source concepts:

Activity ID
Activity Name
Start
Finish

Optional:

Description
WBS
Location
Quantity
Unit

If required columns are absent, reject the file with a useful error explaining which required columns are missing.

Do not silently import a file that cannot produce meaningful activities.

BASIC ROW HANDLING

For each parsed row:

trim obvious surrounding whitespace;
convert empty strings to null where the database permits null;
preserve source values otherwise;
map them into the canonical import representation.

Do NOT perform deep normalization.

Do NOT change arbitrary date formats into a universal format in this pass unless the parser already gives a reliable date value.

Do not infer missing activity IDs or names.

Rows missing required activity identity fields should be rejected or reported clearly.

Prefer a deterministic import result rather than silently dropping bad rows.

SCHEDULE RECORD

When importing a file, create one schedules record representing that import.

Populate sensible fields:

generated schedule ID;
current project ID;
schedule name;
version;
source type (csv or xlsx);
original filename;
baseline flag using the existing schema conventions.

Do not invent sophisticated schedule-versioning logic yet.

A simple imported schedule record is sufficient for this pass.

ACTIVITY PERSISTENCE

Each canonical import row becomes an activities record linked to:

current project
+
new schedule

Map:

externalId    → external_id
name          → name
description   → description
wbsCode       → wbs_code
location      → location
start         → planned_start
finish        → planned_finish
quantity      → planned_quantity
unit          → unit

Use the existing database constraints.

Do not bypass the repository layer.

REPOSITORIES

Introduce repository boundaries for the persistence operations actually required by this pass.

At minimum provide the ability to:

create schedule
retrieve schedule
list schedules for project

create activities for schedule
retrieve/list activities for schedule

Use typed repository interfaces.

The exact names should match the existing project repository style.

Do not implement the full future schedule/activity CRUD surface unless genuinely necessary.

TRANSACTIONAL IMPORT

The import must be atomic.

Use the existing transaction boundary.

The desired behavior is:

create schedule
    ↓
insert activities
    ↓
all succeed
    ↓
commit

If any required row or database operation fails:

rollback

The database must not be left with:

schedule created
but only half of its activities imported

Test this explicitly.

DUPLICATES

The existing database already protects:

(schedule_id, external_id)

Do not invent a new duplicate policy.

For a duplicate activity ID within the same imported schedule, fail the import clearly rather than silently overwriting data.

Do not attempt merge/upsert behavior in Pass 4.

IMPORT SERVICE

Create an application service responsible for orchestration.

Conceptually:

ScheduleImportService

It should:

identify the parser;
parse the file;
map headers;
perform basic structural checks;
create the schedule;
create activities;
commit atomically;
return an import summary.

The service must not depend on Express request/response objects.

IMPORT RESULT

Return a useful structured result such as:

schedule
activitiesImported
sourceType
originalFilename

Optionally include:

rowCount

Do not build a giant import-reporting framework.

HTTP API

Add a project-scoped schedule import endpoint.

Suggested shape:

POST /api/projects/:projectId/schedules/import

Use multipart form-data with a single file field, for example:

file

Validate:

project ID;
file presence;
supported file type;
file size.

The route should remain thin and delegate to the import service.

Also expose a small read endpoint so the frontend can verify the result, for example:

GET /api/projects/:projectId/schedules
GET /api/projects/:projectId/schedules/:scheduleId/activities

Keep these read endpoints minimal.

They exist to support the importer workflow and demo verification.

FRONTEND

Add an Import Schedule capability to the currently open project workspace.

Do not replace the entire Pass 3 UI.

Add only what is needed.

The user flow should be:

Open project
    ↓
Import Schedule
    ↓
Choose CSV/XLSX file
    ↓
Upload
    ↓
Import progress/state
    ↓
Success summary
    ↓
View imported activities

The UI should show:

selected filename;
supported formats;
upload state;
success/failure;
activities imported.

After successful import, show a basic activity table containing at least:

Activity ID
Activity Name
WBS
Location
Start
Finish
Quantity
Unit

This table is a verification surface, not the final Activity Detail UI.

Do NOT implement sorting, filtering, editing, progress, variance, risk, or AI matching yet.

ERROR HANDLING

Use the existing AppError model.

Expected importer errors include:

unsupported file type;
missing file;
file too large;
missing required headers;
malformed CSV;
malformed XLSX;
invalid row structure;
duplicate activity within import;
unknown project;
database failure.

Return clear human-readable errors.

Do not expose raw parser stack traces.

CLEANUP

Temporary uploaded files should be cleaned up after:

successful import;
failed import.

Do not leave uploaded junk inside the repository.

Permanent evidence/document storage belongs to later passes.

TESTING

Add tests for:

Parser tests

CSV:

valid file;
common headers;
optional columns;
missing required header;
malformed input.

XLSX:

valid workbook;
expected worksheet parsing;
missing required header.
Header mapping

Verify supported aliases map to the same canonical fields.

Import service
creates schedule;
creates activities;
links everything to the current project;
returns import summary.
Transaction behavior

Verify:

successful import commits;
failed import rolls back schedule and activities.
Duplicate behavior

Duplicate activity IDs within one schedule must fail predictably.

API tests

Test:

POST /api/projects/:projectId/schedules/import
GET /api/projects/:projectId/schedules
GET /api/projects/:projectId/schedules/:scheduleId/activities

including validation/error cases.

Regression

All Pass 0–3 tests must remain green.

TEST DATA

Create small deterministic test fixtures for both CSV and XLSX.

Keep them under a suitable test-fixture location.

Do not add large proprietary sample files.

The fixtures should cover:

Activity ID
Activity Name
Description
WBS
Location
Start
Finish
Quantity
Unit

and at least one reasonable alias variation.

DATABASE SAFETY

Do not modify the migration history.

Do not create a new migration unless a concrete schema change is actually required.

The existing schedules and activities schema should already be sufficient for this pass.

If you believe a schema change is genuinely necessary, STOP and explain why in the final report rather than silently expanding the scope.

PASS BOUNDARY CHECK

Before committing, explicitly verify that you did NOT implement:

schedule normalization;
schedule validation engine;
AI extraction;
activity matching;
progress reporting;
variance;
risk;
evidence/document ingestion;
assistant;
dashboard analytics.

Those are later passes.

VALIDATION

Run:

npm test
npm run build
npm run setup
npm run setup
npm run demo:reset

Then manually verify:

1. Open FieldLine.
2. Open an existing project.
3. Import a real CSV schedule.
4. Confirm successful import.
5. Confirm activities appear.
6. Import a real XLSX schedule.
7. Confirm successful import.
8. Confirm activities appear.
9. Try an unsupported file.
10. Confirm a clear error.
11. Try a file missing a required header.
12. Confirm a clear error.
13. Confirm /api/health remains healthy.

Use real local fixture files during verification.

SELF-REVIEW

Inspect the final diff and ask:

Architecture

Does the importer follow:

route
→ validation
→ import service
→ parser
→ repository
→ SQLite
Persistence

Is the entire import atomic?

Parsing

Do CSV and XLSX produce the same canonical representation?

Scope

Did anything from Pass 5 or Pass 6 accidentally get implemented?

Regression

Does the complete existing project lifecycle still work?

Local-only architecture

Did we introduce any external storage/deployment service?

The answer must be no.

GIT

Inspect:

git status
git diff

Confirm only PASS 4 work is included.

Create:

feat: implement FieldLine schedule importer

Push to:

itshim998/FieldLine
main
COMPLETION CRITERIA

PASS 4 is complete only when:

 CSV upload works.
 XLSX upload works.
 required headers are detected.
 common header aliases work.
 parser output is format-independent.
 schedule record is created.
 activity records are created.
 activities belong to the selected project.
 imports are transactional.
 failed imports leave no partial schedule/activity state.
 duplicate activity IDs are handled deterministically.
 import API works.
 imported schedules can be listed.
 imported activities can be viewed.
 frontend import flow works.
 unsupported files fail clearly.
 missing required headers fail clearly.
 temporary files are cleaned up.
 Pass 0–3 behavior remains intact.
 npm test passes.
 npm run build passes.
 setup passes.
 demo reset passes.
 health endpoint remains healthy.
 no Pass 5+ functionality was implemented.
 final diff reviewed.
 commit created.
 commit pushed.
FINAL REPORT

Report:

Files added/changed.
Parser architecture.
Header aliases supported.
Schedule/activity repository changes.
Import service flow.
API endpoints.
Frontend import flow.
Transaction/rollback behavior.
Tests added and total passing tests.
Results of all validation commands.
Manual CSV/XLSX verification.
Any schema changes and why.
Self-review findings.
Commit hash/message/branch/push status.

Explicitly confirm:

PASS 5 and later functionality were NOT implemented.

FINAL INSTRUCTION

Implement PASS 4 — Schedule Importer only.

The goal is simple:

A real CSV or XLSX schedule file should enter an open FieldLine project and become usable persisted activities.

Do not jump ahead into normalization, validation engines, AI, progress, or analytics.

Stop after PASS 4.
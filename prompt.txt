You have already completed the Supabase/PostgreSQL migration.

Do NOT perform another database migration.
Do NOT redesign the dual-engine architecture.
Do NOT redo Phase 4.

The current authoritative repository state is GitHub `main` commit:

```text
8d09ef6d1ed9a4c9346c9649bb7335c27c09338d
```

Commit:

```text
feat(db): migrate production database to Supabase PostgreSQL with dual-engine provider and full regression preservation
```

I need a **small, targeted post-migration correctness fix** for the existing Phase 4 notification implementation.

---

# CRITICAL ISSUE 1 — WRONG `projectId`

Inspect:

```text
backend/src/services/anomaly/anomaly-notification.service.ts
```

The current implementation still passes:

```ts
projectId: messageInput.projectName
```

and:

```ts
projectId: context.projectName
```

into notification outbox creation.

This is incorrect.

`notification_outbox.project_id` is a relational identifier and must contain the real:

```text
projects.id
```

not:

```text
projects.name
```

The implementation must obtain the authoritative `projectId` from the actual server-side report/match context.

### Required behavior

```text
trusted projectId
      ↓
notification_outbox.project_id
```

Never:

```text
projectName
      ↓
project_id
```

Do not derive IDs from display names.

Do not introduce a second project lookup merely because it is easy unless the existing context architecture requires it.

Prefer passing the authoritative existing `projectId` through the notification context.

---

# CRITICAL ISSUE 2 — NEVER FABRICATE `activityMatchId`

The current implementation also contains:

```ts
const matchId = context.activityMatchId || `match_${Date.now()}`;
```

This is invalid.

`notification_outbox.activity_match_id` is a foreign key to:

```text
activity_matches.id
```

Therefore an arbitrary synthetic identifier must never be inserted.

### Required behavior

```text
real persisted activityMatchId
      ↓
notification_outbox.activity_match_id
```

If the authoritative activity match ID is unavailable:

```text
DO NOT CREATE THE NOTIFICATION INTENT.
```

Do not generate:

* timestamps
* random strings
* fake `match_*` values
* activity names as IDs
* project names as IDs

The notification system must never contain an orphaned reference.

---

# IMPORTANT: APPLY THIS TO ALL PHASE 4 CREATION PATHS

Do not fix only one occurrence.

Audit:

```text
recordNotificationIntent()
notifyAnomalyAlert()
```

and every other caller that creates notification outbox entries.

Search the repository for:

```text
outboxRepo.create
notificationOutboxRepo.create
CreateNotificationOutboxInput
projectId:
activityMatchId:
fieldline-anomaly-alert:
```

Confirm that every notification creation path uses the real persisted identifiers.

---

# IMPORTANT: RESPECT THE NEW POSTGRESQL FOREIGN KEYS

The repository now has PostgreSQL-backed implementations.

Therefore this must be true for:

```text
DATABASE_PROVIDER=postgres
```

as well as SQLite.

Verify that the notification path passes:

```text
projects.id
activity_matches.id
```

that actually exist in the active database.

Do not make SQLite-specific assumptions.

Do not weaken or remove foreign-key constraints.

Do not make PostgreSQL more permissive merely to hide invalid notification input.

---

# DO NOT CHANGE THE PHASE 2 CONTRACT UNNECESSARILY

Phase 2 currently provides contextual values such as:

```text
projectName
activityExternalId
activityName
...
activityMatchId
```

Do not redesign the Phase 2 message-generation contract just to solve this.

The correction should establish the proper distinction:

```text
display context:
projectName

relational identity:
projectId
```

If the current notification context lacks `projectId`, add the smallest explicit server-owned field required to carry it through the notification boundary.

Do not infer it from `projectName`.

---

# IMPORTANT: EXISTING POSTGRESQL / SQLITE DUAL-ENGINE ARCHITECTURE

The current repository uses both:

```text
SQLite implementations
PostgreSQL implementations
```

with provider selection.

Preserve this architecture.

Do not:

* remove SQLite
* remove PostgreSQL
* replace the repository system
* add an ORM
* rewrite all repositories
* change unrelated async behavior

Only make the changes required to correctly carry authoritative identifiers through the notification pipeline.

---

# VERIFICATION 1 — DUPLICATE / INVALID IDENTIFIER TESTS

Add regression tests proving:

### Valid

```text
projectId = real project ID
activityMatchId = real activity match ID
```

→ notification outbox row is successfully created.

### Invalid project identity

A display name passed where a project ID is expected must not silently create a valid notification.

### Missing activity match

If:

```text
activityMatchId = null / undefined
```

→ no notification outbox row is created.

### Fabricated match

Verify the code cannot produce:

```text
match_<timestamp>
```

or another synthetic match ID.

---

# VERIFICATION 2 — POSTGRESQL-SPECIFIC TEST

Run the notification persistence test path against the PostgreSQL implementation.

Verify:

```text
real project ID
+
real activity match ID
↓
Postgres notification_outbox
```

and confirm PostgreSQL foreign-key integrity succeeds.

Also verify that the previous incorrect behavior:

```text
projectName → project_id
```

fails rather than silently succeeding.

Do not only test SQLite.

---

# VERIFICATION 3 — FULL END-TO-END ANOMALY PATH

Exercise the real flow:

```text
worker report
    ↓
Phase 1 anomaly
    ↓
activity match persisted
    ↓
Phase 4 notification creation
    ↓
notification_outbox
```

Verify the resulting outbox record contains:

```text
project_id = actual projects.id
activity_match_id = actual activity_matches.id
```

Then verify Phase 4 delivery still functions.

---

# VERIFICATION 4 — PRESERVE ALL EXISTING PHASE 4 INVARIANTS

Do not regress:

```text
normal/cold-start → no notification

same anomaly/channel → one logical notification

same notification → same idempotency key

retry → same message snapshot

retry → no second Groq generation

temporary failure → retry_wait

permanent failure → failed

stale lease → recoverable

anomaly persistence is independent from notification delivery
```

---

# IMPORTANT: CHECK THE SUPABASE DATABASE INITIALIZATION

Because PostgreSQL is now available, verify that the corrected notification path is actually backed by the PostgreSQL repository when:

```text
DATABASE_PROVIDER=postgres
```

and not accidentally by the SQLite implementation.

You do not need to redesign the current repository-provider selection mechanism.

Just prove the correct implementation is selected.

---

# SECURITY / DATA INTEGRITY

Do not weaken:

* foreign keys
* unique notification constraints
* idempotency keys
* transaction boundaries

Do not convert identifiers into human-readable strings.

A display name and a relational ID are different concepts and must remain different throughout the application.

---

# TESTING COMMANDS

Run the focused notification tests.

Run PostgreSQL-specific tests using a test database/configuration where practical.

Then run:

```bash
npx tsc -p tsconfig.backend.json --noEmit
npm test
```

Do not claim success unless the commands actually pass.

---

# FINAL REPORT

Return:

### 1. Exact bug fixed

Show where `projectName` was incorrectly being used as `projectId`.

### 2. Exact identifier flow now

Show:

```text
real projectId → notification_outbox.project_id
real activityMatchId → notification_outbox.activity_match_id
```

### 3. Missing-ID behavior

Explain exactly what happens when `activityMatchId` is unavailable.

### 4. PostgreSQL verification

Confirm the corrected notification path was tested against PostgreSQL/Supabase-compatible persistence.

### 5. Regression tests

Give exact commands and actual pass/fail counts.

### 6. Full test suite

Give the actual final result.

Do not perform unrelated refactoring.

This is a **targeted correctness correction after the Supabase migration**, not a new architecture phase.

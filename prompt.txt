# FieldLine — PASS 7: Manual Progress Reporting

You are implementing PASS 7 — Manual Progress Reporting.

FieldLine repository:

itshim998/FieldLine

Local directory:

C:\Users\Sounak\Desktop\Flagship AI projects\FieldLine

PASS 0 through PASS 6 are complete.

Before coding, inspect the current repository and build this pass on top of the existing architecture.

---

# CURRENT STATE

The Pass 2 database already contains the `progress_updates` table.

It already supports:

- project_id
- report_date
- reporter_name
- reporter_role
- source_type
- raw_text
- status
- created_at
- updated_at

The existing `source_type` domain includes:

```text
manual
voice
pdf
xlsx
image
text

The existing status domain includes:

received
processed
reviewed

Do NOT create a new migration unless the repository inspection proves one is genuinely required.

Do NOT redesign the existing schema.

OBJECTIVE

Build the simplest working manual field-update workflow before introducing AI.

A supervisor should be able to enter something like:

Foundation work at Block B is 60% complete. Concrete pouring started today.

FieldLine must persist:

raw update text;
report date;
timestamp;
reporter;
project;
source type;
status.
EXIT CRITERION

A manually entered progress update can be:

created
→ persisted
→ retrieved
→ displayed

inside the currently selected FieldLine project.

CRITICAL PASS BOUNDARY

This pass is RAW MANUAL REPORTING ONLY.

Do NOT implement:

AI extraction;
activity matching;
activity progress calculation;
actual percent updates;
actual quantity calculation;
planned vs actual;
variance;
delay detection;
risk;
evidence upload;
document ingestion;
voice transcription;
automatic progress inference.

A sentence containing:

"60% complete"

must remain part of raw_text.

Do NOT write 60 into:

activity_progress.actual_percent

Do not modify activities as a result of manual report submission.

The only persistence target for this pass is the progress_updates workflow.

FIRST: INSPECT THE CURRENT REPOSITORY

Inspect:

backend/src/models/domain.types.ts
database schema/migrations
repository conventions
service conventions
route conventions
Zod validation conventions
AppError handling
project workspace frontend
existing schedules/activity UI
existing project-scoped API patterns
test conventions

In particular, reuse:

ProjectService / ProjectRepository patterns
ScheduleImportService / ScheduleRepository patterns

Do not invent a second architecture.

TARGET ARCHITECTURE

Use:

React UI
    ↓
HTTP route
    ↓
Zod validation
    ↓
ProgressUpdateService
    ↓
ProgressUpdateRepository
    ↓
SQLite

Routes must remain thin.

Services must not depend on Express objects.

Repositories must contain the SQL.

Frontend must use the API and must not access SQLite directly.

1. PROGRESS UPDATE DOMAIN TYPES

The existing domain already contains:

ProgressUpdate
CreateProgressUpdateInput

Reuse those types.

Do not create duplicate models.

If additional DTOs are genuinely required for API responses, introduce them cleanly and minimally.

2. PROGRESS UPDATE REPOSITORY

Create:

backend/src/repositories/progress-update.repository.ts

using the same interface/implementation pattern as the existing repositories.

At minimum support:

create()
getById()
listByProjectId()
countByProjectId()

Optionally add delete() only if the current UI genuinely needs it.

Do not implement a large future CRUD surface.

The repository owns all direct SQL access.

3. CREATE BEHAVIOR

Creating a manual progress update should populate:

project_id       → current project
report_date      → supplied report date
reporter_name    → supplied reporter name
reporter_role    → supplied reporter role, if entered
source_type      → "manual"
raw_text         → exact user-entered update text
status           → "received"

Do not silently rewrite the raw report text.

The raw text is valuable source evidence for later AI extraction.

Preserve it exactly apart from whatever transport serialization is unavoidable.

4. TIMESTAMP SEMANTICS

The database already provides:

created_at
updated_at

Use those as the persisted timestamp fields.

Do not invent another custom timestamp column.

The API response should expose the existing timestamps.

For a newly created update:

created_at
≈
updated_at

Use the existing SQLite timestamp conventions.

5. REPORT DATE

The update should have a separate:

reportDate

representing the date the field report refers to.

This is distinct from:

createdAt

which represents when FieldLine received/stored it.

The UI should allow the user to specify report date.

Default it to the current local date where appropriate, but do not silently confuse:

report date

with:

database creation timestamp
6. REPORTER

Allow the manual reporter to provide:

reporterName
reporterRole

Reporter role is optional.

Do not implement user authentication or account management.

The reporter identity is simply data stored with the manual report for this pass.

7. VALIDATION

Create Zod request validation.

At minimum:

Create progress update
rawText
reportDate
reporterName
reporterRole

Rules:

rawText required;
trim surrounding whitespace for validation;
reject empty/whitespace-only raw text;
reasonable maximum length to prevent accidental massive payloads;
reportDate must be a valid canonical date;
reporterName optional but, if supplied, must not be whitespace-only;
reporterRole optional.

Do NOT normalize the actual stored rawText.

For example:

" Foundation work at Block B is 60% complete. "

may be validated as non-empty but the stored value should retain the user's entered text rather than being aggressively rewritten.

Use the existing validation/error architecture.

8. DATE HANDLING

Use the existing project date conventions.

Do not create another date-normalization system.

For the manual report date, accept a simple canonical date from the UI:

YYYY-MM-DD

and validate it cleanly.

Do not introduce natural-language date parsing.

Do not interpret:

"today"

inside the backend as a special semantic language feature.

The frontend may default the input to today's date.

9. API

Create project-scoped progress-update endpoints.

At minimum:

POST /api/projects/:projectId/progress-updates
GET  /api/projects/:projectId/progress-updates
GET  /api/projects/:projectId/progress-updates/:updateId

The POST endpoint creates a manual update.

The backend must force:

sourceType = "manual"
status = "received"

Do NOT allow the client to spoof these fields.

The GET collection should return project-scoped updates, preferably newest first by report/creation time in a deterministic way.

The GET-by-ID endpoint must verify that the update belongs to the requested project.

An update from another project must not be exposed.

10. SERVICE

Create:

ProgressUpdateService

with a minimal application surface such as:

createManualUpdate()
listProjectUpdates()
getProjectUpdate()

The service should:

verify the project exists;
validate/application-check the relevant input;
force manual source/status semantics;
call the repository;
optionally record a project event;
return the persisted update.

Do not put SQL into the service.

Do not put Express objects into the service.

11. PROJECT EVENTS

The existing domain already contains a project event type:

progress_reported

Use it where appropriate.

A successful manual update can create a project event such as:

event_type = progress_reported
entity_type = progress_updates
entity_id = <update ID>
summary = Manual progress report received

If an event is created, keep it inside the same logical operation as the progress update.

Do not create a general event framework.

Do not make event creation more complicated than necessary.

If the existing project/event conventions make this unnecessary for Pass 7, explain the decision in the final report.

12. TRANSACTION / ATOMICITY

A manual progress update is a small operation.

If both:

progress_updates
project_events

are written, they should be atomic.

Use the existing SQLite transaction mechanism where appropriate.

If only progress_updates is written, the repository operation itself is enough.

Do not build a large transaction abstraction.

13. FRONTEND

Add a simple manual progress reporting workflow to the currently open project workspace.

The current project workspace already contains future sections for later functionality.

Make the Progress Updates area functional for PASS 7.

The user flow should be:

Open Project
    ↓
Progress Updates
    ↓
Enter Field Update
    ↓
Submit
    ↓
Success
    ↓
Update appears in history/timeline
14. MANUAL UPDATE FORM

Create a simple form with:

Report date

Date input.

Default to today's local date.

Reporter name

Text input.

Reporter role

Optional text input.

Field update

Large textarea.

Example placeholder:

Foundation work at Block B is 60% complete.
Concrete pouring started today.

Submit button:

Record Field Update

Do not add AI extraction controls yet.

Do not ask the user for activity ID.

Do not ask for percentage as a separate field.

The entire purpose is to preserve the raw human report.

15. UPDATE HISTORY

After submission, display the stored progress updates for the current project.

Each update should show at minimum:

report date;
created timestamp;
reporter name;
reporter role if available;
source badge: Manual;
raw update text;
status badge: Received.

Display newest updates first.

Keep the UI presentation-grade but simple.

No charts.

No progress bars derived from the text.

No activity linking.

16. CURRENT PROJECT

Use the existing Pass 3 current-project context.

The progress update API must always operate against the selected project.

Do not introduce another project selector specifically for progress updates.

If no project is currently open, the existing project-selection flow should remain responsible for resolving that state.

17. ERROR HANDLING

Use the existing AppError architecture.

Handle:

project not found;
update not found;
invalid report text;
invalid report date;
invalid reporter fields;
database failure.

Return useful errors to the frontend.

Do not expose raw SQLite errors.

18. TESTING

Add dedicated repository tests:

Repository
create manual progress update;
retrieve by ID;
list by project;
count by project;
project scoping;
timestamps populated.

Add service tests:

project existence;
source type forced to manual;
status forced to received;
raw text preserved;
reporter stored;
project event behavior if implemented.

Add router/API tests:

POST /api/projects/:projectId/progress-updates
GET /api/projects/:projectId/progress-updates
GET /api/projects/:projectId/progress-updates/:updateId

Test:

valid creation;
empty raw text;
invalid date;
missing project;
cross-project access;
list ordering.
19. FRONTEND VALIDATION

Test or manually verify:

1. Open a project.
2. Open Progress Updates.
3. Enter a manual report.
4. Submit it.
5. Confirm success.
6. Confirm the report appears in history.
7. Refresh the browser.
8. Confirm the report still appears.
9. Enter another report.
10. Confirm newest-first ordering.

Do not implement actual progress calculations from the submitted text.

20. RAW TEXT INTEGRITY

This is important.

Suppose the user submits:

Foundation work at Block B is 60% complete. Concrete pouring started today.

The database must store exactly that report as the raw_text.

Do not:

extract "60";
rewrite the sentence;
split it into activities;
assign it to an activity;
create activity_progress;
create activity_matches.

Those belong to later passes.

21. PASS 8+ BOUNDARY

Before committing, explicitly verify that you did NOT implement:

AI extraction;
activity matching;
progress normalization;
actual progress calculation;
variance;
risk;
evidence/document processing;
assistant.

This pass exists specifically to establish a reliable human-entered raw report that later AI functionality can consume.

22. DATABASE

Do NOT modify the migration chain unless a concrete schema deficiency is discovered.

The existing progress_updates table should already be sufficient.

Do not create:

manual_progress_updates
field_reports
raw_reports

or another duplicate table.

progress_updates is the source of truth.

23. DOCUMENTATION

Update:

docs/ARCHITECTURE.md

to show:

Manual Progress UI
    ↓
Progress Update API
    ↓
ProgressUpdateService
    ↓
ProgressUpdateRepository
    ↓
SQLite progress_updates

Also document that:

Manual reports are stored as raw text in Pass 7. No AI extraction or activity progress mutation occurs yet.

Update the README only enough to mark Pass 7 complete.

Do not alter the authoritative 26-pass roadmap.

24. SELF-REVIEW

Before committing, inspect the final implementation.

Architecture

Is it:

UI
→ route
→ validation
→ service
→ repository
→ SQLite
Data

Does the stored record preserve:

raw text;
report date;
reporter;
project;
source type;
status;
timestamps?
Project isolation

Can one project's update be retrieved through another project's endpoint?

It must not.

Raw text

Is the original report preserved exactly?

Scope

Did any AI/progress calculation/matching logic leak into this pass?

Regression

Do Pass 0–6 workflows still work?

VALIDATION COMMANDS

Run:

npm test
npm run build
npm run setup
npm run setup
npm run demo:reset

Then run:

npm run dev

Manually verify:

Create/open a project.
Open Progress Updates.

Enter:

Foundation work at Block B is 60% complete. Concrete pouring started today.

Submit it.
Verify it appears in the update history.
Verify sourceType = manual.
Verify status = received.
Verify the exact raw text is preserved.
Verify reporter and report date are displayed.
Refresh and confirm persistence.
Create a second update and verify newest-first ordering.
Verify /api/health remains healthy.
GIT

Inspect:

git status
git diff

Confirm only PASS 7 work is included.

Do not commit:

.env
SQLite runtime files
WAL/journal files
dist
uploads
secrets
unrelated prompt artifacts if avoidable

Create:

feat: implement FieldLine manual progress reporting

Push to:

itshim998/FieldLine
main
COMPLETION CRITERIA

PASS 7 is complete only when:

 ProgressUpdateRepository exists.
 ProgressUpdateService exists.
 Zod request validation exists.
 Manual progress API exists.
 Project-scoped retrieval works.
 Raw update text is preserved.
 Report date is stored.
 Reporter name is stored.
 Reporter role can be stored.
 Source type is forced to manual.
 Status is initially received.
 Timestamps are persisted.
 Project isolation is enforced.
 Progress Update history is displayed.
 Refresh preserves the history.
 No actual activity progress is mutated.
 No AI extraction exists.
 No activity matching exists.
 Pass 0–6 functionality remains intact.
 npm test passes.
 npm run build passes.
 setup passes.
 repeated setup passes.
 demo reset passes.
 health endpoint remains healthy.
 manual browser flow verified.
 final diff reviewed.
 commit created.
 commit pushed.
FINAL REPORT

Report:

Current repository architecture used.
ProgressUpdateRepository implementation.
ProgressUpdateService implementation.
API endpoints.
Validation rules.
Raw text preservation behavior.
Reporter/timestamp/source/status behavior.
Project isolation behavior.
Frontend progress-update workflow.
Tests added and total passing tests.
Validation command results.
Manual browser verification.
Any schema changes and why.
Self-review findings.
Commit hash/message/branch/push result.

Explicitly confirm:

PASS 8 and later functionality were NOT implemented.

FINAL INSTRUCTION

Implement PASS 7 — Manual Progress Reporting only.

The goal is:

A supervisor can enter a raw field update, FieldLine stores it reliably against the correct project, and the user can immediately see the persisted update.

Do not interpret the report yet.

Stop after PASS 7.
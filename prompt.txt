You are performing the **final closure and certification sequence for the FieldLine ML subsystem**.

# FIELDLINE ML — PHASES 31–35

## AUTHORITATIVE PLAN

The authoritative ML implementation blueprint is:

```text
ChatGPT_plan.md
```

Phases **1–30 are already implemented and verified**.

The remaining phases are:

```text
Phase 31 → Canonical Truth Boundary Certification
Phase 32 → Semantic Versioning
Phase 33 → Retraining Workflow
Phase 34 → Prohibited Technologies Audit
Phase 35 → Final Definition of Done
```

These phases represent the **closure of the current ML implementation**.

Do not invent Phase 36+ ML functionality.

Do not add a new model.

Do not redesign the existing models.

Do not “improve” model performance merely because optimization is possible.

The objective is to **prove, document, and freeze the existing architecture**.

---

# MANDATORY EXECUTION RULE

Even though this prompt contains Phases 31–35, execute them strictly in order:

```text
PLAN
  ↓
IMPLEMENT
  ↓
EVALUATE
  ↓
VERIFY
  ↓
PHASE EXIT
  ↓
NEXT PHASE
```

Do not proceed to the next phase until the current phase has been verified.

Do not combine unrelated changes.

---

# CURRENT CERTIFIED ML ARCHITECTURE

FieldLine uses a strict dual-layer architecture.

## Probabilistic layer

```text
Gemini / Groq / MockAI
        ↓
structured field facts

Match Model
        ↓
candidate reranking / confidence

Anomaly Model
        ↓
review-prioritization advisory
```

## Deterministic authoritative layer

```text
human-confirmed result
        ↓
ProgressService.normalizeAndRecordProgress()
        ↓
activity_progress
        ↓
deterministic snapshots / variance / risk
```

---

# NON-NEGOTIABLE INVARIANTS

These must remain true throughout Phases 31–35.

## Canonical Truth

```text
ML prediction != canonical truth
```

Neither ML model may directly mutate:

```text
activity_progress
```

Only the existing verified canonical progress pathway may write to `activity_progress`.

The Match Model, Anomaly Model, artifact verifier, documentation, and retraining workflow must never bypass this boundary.

---

## Match Model

The Match Model remains a **second-stage reranker**.

The existing deterministic matcher:

```text
backend/src/services/matching/activity-match-scoring.ts
```

must remain intact.

Do not replace deterministic candidate generation with ML.

For non-exact matches, preserve:

```text
deterministic score >= 0.90
AND
ML confidence >= 0.85
AND
candidate separation >= 0.15
```

ML confidence alone can never auto-confirm.

Exact external-ID matches retain their deterministic path.

---

## Anomaly Model

The Anomaly Model remains:

```text
unsupervised statistical anomaly detector
+
advisory review prioritization
```

It is NOT a:

```text
fraud detector
lie detector
worker honesty detector
authenticity classifier
```

---

## Production Runtime

Production remains:

```text
Node.js 22
TypeScript
Express
Better-SQLite3
native TypeScript ML inference
small JSON artifacts
```

Offline training remains:

```text
Python 3
NumPy
```

Do not introduce:

```text
PyTorch
TensorFlow
Keras
CUDA
GPU runtime
vector database
embedding infrastructure
Python server
remote ML inference service
LLM fine-tuning
```

---

# INITIAL RECONNAISSANCE

Before making any changes:

```bash
git status
git diff
git log --oneline -20
```

Inspect:

```text
ChatGPT_plan.md
backend/src/ml/
backend/src/ml/artifacts/
backend/src/ml/match/
backend/src/ml/anomaly/
scripts/ml/
scripts/verify-release.ts
package.json
backend/src/services/ProgressService* or equivalent
activity_progress repository/write paths
migration 0010
docs/ML-ARCHITECTURE.md
docs/ML-TRAINING.md
```

Determine the actual current state.

Do not delete/reset/revert user changes.

Do not assume the repository exactly matches an earlier walkthrough.

---

# PHASE 31 — CANONICAL TRUTH BOUNDARY CERTIFICATION

## OBJECTIVE

Machine-prove that the ML subsystem cannot silently become a second source of canonical project truth.

The required architectural claims are:

```text
activity_progress is append-only
```

and:

```text
only ProgressService.normalizeAndRecordProgress()
can write to activity_progress
```

and:

```text
ML predictions are advisory signals only
```

---

## Phase 31 — PLAN

Identify every code path that can potentially mutate:

```text
activity_progress
```

Search the entire backend codebase.

Do not rely only on filenames.

Search for:

```text
INSERT INTO activity_progress
UPDATE activity_progress
DELETE FROM activity_progress
activityProgressRepository
normalizeAndRecordProgress
progress writes
```

Build a concise write-path inventory.

The goal is to establish:

```text
ALL activity_progress write paths
```

and verify that they converge on the intended canonical service.

---

## Phase 31 — IMPLEMENT

Only make changes if necessary to close a genuine architectural hole.

Prefer certification tests and static checks over refactoring existing working code.

Create or strengthen a dedicated test suite if useful, e.g.:

```text
backend/tests/ml_canonical_truth_boundary.test.ts
```

Test at minimum:

### Test A — ML inference alone

Run Match Model / Anomaly Model inference.

Assert:

```text
activity_progress
```

is unchanged.

### Test B — ambiguous/review state

Run the ML pipeline for an ambiguous candidate.

Assert:

```text
reviewState === 'awaiting_review'
```

and canonical progress remains unchanged.

### Test C — anomaly warning

Run an anomalous update through the pre-review pathway.

Assert:

```text
anomalyScore / severity / reasons
```

are advisory and do not mutate canonical progress.

### Test D — supervisor confirmation

Perform the existing human confirmation flow.

Assert canonical progress changes only through the existing canonical service.

Do not create a second canonical write mechanism.

### Test E — rejection

Reject a proposed match.

Assert that no unauthorized canonical progress mutation occurs.

---

## Phase 31 — Stronger Static Boundary Audit

Where practical, implement a lightweight machine-checkable assertion that canonical `activity_progress` writes are limited to the approved service/repository path.

Do NOT build a complicated static-analysis framework.

A simple deterministic repository-level audit is preferable.

The certification should prove the architectural rule as strongly as practical.

---

## Phase 31 — Evaluate

Run:

```bash
npm test
npm run ml:verify
npm run verify:release
```

and all targeted canonical-truth tests.

Verify before/after database state where applicable.

---

## Phase 31 — EXIT CRITERIA

* [ ] All `activity_progress` write paths have been audited.
* [ ] Canonical writes converge on the approved service/path.
* [ ] ML inference cannot mutate canonical progress.
* [ ] Awaiting-review ML results cannot mutate canonical progress.
* [ ] Anomaly evaluation cannot mutate canonical progress.
* [ ] Human confirmation uses the existing canonical pathway.
* [ ] Rejection does not mutate canonical progress.
* [ ] Tests pass.
* [ ] Release verification passes.
* [ ] No unnecessary domain refactoring occurred.
* [ ] No Phase 32+ functionality was implemented prematurely.

---

# PHASE 32 — SEMANTIC VERSIONING

## OBJECTIVE

Certify that the model artifacts and their feature contracts have explicit semantic versions and that the production loaders verify compatibility.

The existing blueprint specifies:

```text
match-model.json   → version 1.0.0
anomaly-model.json → version 1.0.0
```

and requires feature-contract version checking on backend startup.

---

## Phase 32 — PLAN

Inspect:

```text
backend/src/ml/types.ts
backend/src/ml/artifacts/match-model.json
backend/src/ml/artifacts/anomaly-model.json
MatchModelService
AnomalyModelService
startup/bootstrap code
```

Determine exactly where artifact versions are currently validated.

Do not assume “version field exists” means version compatibility is enforced.

---

## Phase 32 — IMPLEMENT

Verify both artifacts expose:

```text
version
```

and that the production loaders enforce compatibility with the currently supported schema/feature contract.

At minimum:

### Match

Verify:

```text
version === expected supported version
```

and:

```text
featureNames
```

match the supported eight-feature contract exactly.

### Anomaly

Verify:

```text
version === expected supported version
```

and:

```text
featureNames
```

match the supported five-feature contract exactly.

If version validation already exists and is correct, do not rewrite it.

If a genuine gap exists, add the smallest explicit validation mechanism.

---

## Phase 32 — Negative Tests

Test:

```text
unsupported artifact version
```

and verify production loading fails clearly rather than silently accepting an incompatible artifact.

Also test:

```text
feature contract mismatch
```

and verify it is rejected.

Do not silently downgrade or substitute another artifact.

---

## Phase 32 — Important Constraint

Do not bump the versions merely because this phase exists.

The current certified artifacts are:

```text
1.0.0
```

Keep them at `1.0.0` unless the actual contract changes.

A version number should represent a real artifact/contract version, not a phase number.

---

## Phase 32 — Evaluate

Run:

```bash
npm run ml:verify
npm test
npm run verify:release
npm run build:backend
npm run build:frontend
```

---

## Phase 32 — EXIT CRITERIA

* [ ] Match artifact version is explicit.
* [ ] Anomaly artifact version is explicit.
* [ ] Supported feature contracts are explicit.
* [ ] Version compatibility is checked.
* [ ] Feature contract mismatches fail.
* [ ] Unsupported versions fail.
* [ ] No silent version fallback exists.
* [ ] Certified artifacts remain `1.0.0` unless a real contract change exists.
* [ ] Tests/build/release pass.
* [ ] No Phase 33+ functionality implemented prematurely.

---

# PHASE 33 — RETRAINING WORKFLOW

## OBJECTIVE

Create and verify a reproducible, explicit workflow for retraining and safely promoting new ML artifacts.

The intended sequence is:

```bash
npm run ml:train
npm run ml:evaluate
npm run ml:verify
npm test
npm run verify:release
git add backend/src/ml/artifacts/*.json
git commit -m "feat(ml): retrain models on updated golden dataset"
```

The final two git commands are workflow guidance, not permission to automatically commit user work.

Do NOT automatically create a Git commit unless explicitly required by the current repository workflow or user.

---

## Phase 33 — PLAN

Inspect:

```text
scripts/ml/train_match_model.py
scripts/ml/train_anomaly_model.py
scripts/ml/evaluate_models.py
scripts/ml/datasets/
backend/src/ml/artifacts/
package.json
docs/ML-TRAINING.md
```

Determine whether the current workflow is actually reproducible and whether all required steps are documented.

---

## Phase 33 — IMPLEMENT

Ensure the documented retraining workflow is executable and coherent:

```text
train
  ↓
evaluate
  ↓
verify
  ↓
full tests
  ↓
release verification
  ↓
promote artifacts
```

Do not introduce automatic deployment.

Do not introduce CI/CD infrastructure unless already part of the repository.

Do not automatically commit model artifacts.

---

## Phase 33 — Dataset Integrity

Verify that retraining uses the intended datasets.

For Match Model:

* independent labels
* positive pairs
* hard negatives
* soft negatives
* grouped activity holdout

For Anomaly Model:

* normal baseline
* controlled synthetic perturbations
* normal-only fitting where required

Do not alter the datasets merely to improve metrics.

---

## Phase 33 — Retraining Reproducibility

Run:

```bash
npm run ml:train
```

then:

```bash
npm run ml:evaluate
npm run ml:verify
```

Record actual results.

If the artifact bytes are deterministic, report that.

If artifacts differ, explain exactly why.

Do not silently overwrite unexplained changes.

---

## Phase 33 — Promotion Safety

The retraining workflow must make it difficult to promote a bad artifact.

At minimum, verify that:

```text
training
≠ promotion
```

and promotion only occurs after:

```text
evaluation
+
artifact verification
+
tests
+
release verification
```

Do not build an unnecessary artifact registry.

Do not introduce external ML infrastructure.

---

## Phase 33 — Evaluate

Run the full workflow:

```bash
npm run ml:train
npm run ml:evaluate
npm run ml:verify
npm test
npm run verify:release
```

Record exact results.

---

## Phase 33 — EXIT CRITERIA

* [ ] Retraining commands exist and work.
* [ ] Evaluation follows training.
* [ ] ML artifact verification follows evaluation.
* [ ] Full tests follow verification.
* [ ] Release verification follows tests.
* [ ] Dataset integrity is preserved.
* [ ] Grouped activity holdout remains intact.
* [ ] Promotion requires passing verification.
* [ ] No automatic deployment added.
* [ ] No automatic git commit added.
* [ ] Any artifact drift is understood.
* [ ] Reproducibility is documented honestly.
* [ ] No Phase 34+ functionality implemented prematurely.

---

# PHASE 34 — PROHIBITED TECHNOLOGIES AUDIT

## OBJECTIVE

Perform a final architecture audit confirming that the ML subsystem remains intentionally lightweight and local-first.

The following must remain ABSENT from the production architecture:

```text
Pinecone
Chroma
Qdrant
Milvus
PyTorch
TensorFlow
Keras
LLM fine-tuning
Flask
FastAPI
Python production server
GPU/CUDA dependencies
```

---

## Phase 34 — AUDIT

Search the repository for actual imports, dependencies, scripts, configuration, Docker requirements, and startup references.

Do not simply search filenames.

Inspect:

```text
package.json
requirements.txt
pyproject.toml
render.yaml
Dockerfile(s)
backend source
scripts/
deployment config
```

where present.

Distinguish:

```text
offline training tooling
```

from:

```text
production runtime
```

Python and NumPy may legitimately appear in offline ML tooling.

Their existence alone is NOT a violation.

---

## Phase 34 — IMPLEMENTATION

Only remove something if it genuinely violates the prohibited-technology boundary.

Do not delete unrelated libraries merely because a keyword happens to appear in documentation or tests.

If the audit passes already, make no code changes.

Optionally create a concise audit record if useful:

```text
docs/ML-TECHNOLOGY-AUDIT.md
```

Only do so if it provides durable value and does not duplicate existing documentation unnecessarily.

---

## Phase 34 — Evaluate

Run:

```bash
npm run ml:verify
npm test
npm run build:backend
npm run build:frontend
npm run verify:release
```

---

## Phase 34 — EXIT CRITERIA

* [ ] No prohibited production technology is present.
* [ ] No vector database.
* [ ] No heavyweight ML framework.
* [ ] No LLM fine-tuning.
* [ ] No Python production server.
* [ ] No GPU/CUDA dependency.
* [ ] Offline Python/NumPy tooling is clearly separated from production.
* [ ] Tests/build/release pass.
* [ ] No Phase 35+ implementation beyond final certification artifacts.

---

# PHASE 35 — FINAL ML DEFINITION OF DONE

## OBJECTIVE

This is the final closure phase.

The task is to establish that the FieldLine ML subsystem satisfies its complete Definition of Done.

Do NOT introduce a new ML capability.

Do NOT continue feature development after this phase.

---

# PHASE 35 — MASTER CHECKLIST

Verify every item below against the ACTUAL repository.

## Dataset

* [ ] `match_dataset.jsonl` exists.
* [ ] `anomaly_dataset.jsonl` exists.
* [ ] Match dataset uses strict activity holdout.
* [ ] Match labels are independently authored rather than generated from the deterministic matcher.

## Training

* [ ] Match training is from scratch using Python/NumPy.
* [ ] Anomaly training/fitting is offline.
* [ ] JSON artifacts are generated.

## Native Production Inference

* [ ] Match inference runs natively in TypeScript.
* [ ] Anomaly inference runs natively in TypeScript.
* [ ] Production does not require Python.

## Match Model

* [ ] Eight candidate-level features are used.
* [ ] `candidate_rank` is not used.
* [ ] Deterministic candidate generation remains intact.
* [ ] Match Model acts as a second-stage reranker.
* [ ] Exact-ID behavior remains deterministic.
* [ ] Dual-threshold auto-confirm is enforced.
* [ ] ML confidence alone cannot auto-confirm.

## Anomaly Model

* [ ] Anomaly inference occurs pre-review.
* [ ] Five anomaly features remain canonical.
* [ ] Standardized baseline distance is used.
* [ ] `d95` is fitted/exported.
* [ ] `means` are finite.
* [ ] `stds` are finite and strictly positive.
* [ ] Severity thresholds are valid.
* [ ] `distanceThreshold` is absent.
* [ ] Minor negative corrections can remain normal.
* [ ] Anomaly results remain advisory.

## Artifact Safety

* [ ] Artifact schema is validated.
* [ ] Feature order is validated.
* [ ] Feature dimensions are validated.
* [ ] Invalid numerical parameters fail.
* [ ] Unsupported artifact versions fail.
* [ ] No silent repair/defaulting of invalid learned parameters.

## Persistence / API

* [ ] Migration `0010` exists and is valid.
* [ ] Advisory ML metadata remains separate from canonical truth.
* [ ] `/api/ml/status` reports actual model state.
* [ ] Role-specific advisory projection remains intact.

## UI

* [ ] Match confidence appears in existing review cards.
* [ ] Elevated anomaly state appears before confirmation.
* [ ] Activity detail warning is present where intended.
* [ ] No misleading fraud/lie terminology is used.

## Canonical Truth

* [ ] `activity_progress` remains append-only.
* [ ] ML inference cannot mutate it.
* [ ] Only the approved canonical progress service/path can commit progress.
* [ ] Human confirmation remains the review boundary.

## Release Verification

* [ ] `npm run ml:verify` passes.
* [ ] `npm run verify:release` passes with ML verification integrated.

## Runtime / Deployment

* [ ] Node 22 production runtime.
* [ ] No Python runtime in production.
* [ ] Artifacts remain lightweight.
* [ ] Native inference remains lightweight.

## Training Workflow

* [ ] `npm run ml:train` works.
* [ ] `npm run ml:evaluate` works.
* [ ] `npm run ml:verify` works after retraining.
* [ ] Full tests pass.
* [ ] Release verification passes.

---

# PHASE 35 — FINAL MACHINE-CHECKABLE CERTIFICATION

Create a final certification artifact if one does not already exist, for example:

```text
docs/ML-DEFINITION-OF-DONE.md
```

This should contain:

* checklist item
* verification method
* actual status
* evidence/command where useful
* limitation/deviation where applicable

Do not write “PASS” without evidence.

Do not fabricate missing evidence.

---

# PHASE 35 — FINAL REGRESSION

Run the COMPLETE final suite:

```bash
npm run ml:verify
npm test
npm run build:backend
npm run build:frontend
npm run verify:release
```

Also run:

```bash
npm run ml:evaluate
```

only if this reflects the actual current trained artifact state and does not mutate canonical project state.

Where retraining has already been performed in Phase 33, do not unnecessarily retrain again merely to produce another log.

---

# PHASE 35 — FINAL GOLDEN CHECK

Verify the three established REFINERY-U4 ML scenarios one final time where practical:

### Scenario A

```text
Pump foundation piles completed to 65% at Area B crude pump bay
```

Expected architectural behavior:

```text
ACT-B02 is correctly surfaced as the leading candidate.
```

### Scenario B

```text
Crude pump foundation piling jumped to 98% complete today
```

Expected architectural behavior:

```text
elevated anomaly
+
review recommendation
+
canonical progress remains protected before confirmation
```

### Scenario C

```text
Crude pump foundation piles advanced to 68% complete today
```

Expected architectural behavior:

```text
normal / non-alert progression
```

Use actual production inference.

Do not hard-code outputs.

---

# FINAL SCIENTIFIC HONESTY CHECK

Before declaring the subsystem complete, confirm that documentation does NOT overclaim:

Do NOT state:

```text
100% production accuracy
```

when the evidence is only the held-out dataset.

Do NOT state:

```text
fraud detection
```

for the Anomaly Model.

Do NOT state:

```text
83% probability of truth
```

for a raw Match Model probability.

Use accurate language such as:

```text
held-out evaluation performance
learned match probability
statistical anomaly score
review-prioritization signal
```

Document dataset and calibration limitations honestly.

---

# FINAL SCOPE LOCK

After Phase 35:

DO NOT:

* add another model
* tune thresholds without evidence
* expand the feature set
* redesign the deterministic matcher
* introduce forecasting
* introduce vector search
* add cloud ML infrastructure
* add a Python inference service
* add a GPU
* add LLM fine-tuning
* redesign the frontend
* alter canonical truth
* start an unplanned “Phase 36 ML”

The current ML implementation is considered CLOSED once the Definition of Done is satisfied.

---

# FINAL REPORT

Return one comprehensive certification report:

# FIELDLINE ML FINAL CERTIFICATION

## Phase 31 — Canonical Truth Boundary

`COMPLETED` / `BLOCKED`

Report:

* all canonical write paths discovered
* approved write pathway
* ML mutation tests
* human confirmation test
* append-only result

## Phase 32 — Semantic Versioning

`COMPLETED` / `BLOCKED`

Report:

* Match version
* Anomaly version
* feature contract validation
* unsupported-version test
* mismatch rejection behavior

## Phase 33 — Retraining Workflow

`COMPLETED` / `BLOCKED`

Report:

* training commands
* evaluation results
* verification results
* artifact reproducibility/drift
* promotion safety

## Phase 34 — Technology Audit

`COMPLETED` / `BLOCKED`

Report:

* prohibited technologies checked
* production vs offline dependency separation
* any violations discovered
* any removals made

## Phase 35 — Final Definition of Done

`COMPLETED` / `BLOCKED`

Report:

* checklist result
* certification artifact
* final evidence
* remaining limitations

# FINAL SYSTEM METRICS

Report actual current values:

* total test files
* total tests
* Match Top-1
* Match Top-3
* Match ROC-AUC
* artifact sizes
* Match inference benchmark if previously measured
* Anomaly inference benchmark if previously measured
* training runtime if previously measured

Do not manufacture or recompute metrics merely to make them look better.

# FINAL BUILD / RELEASE

Report exact results for:

```text
npm run ml:verify
npm test
npm run build:backend
npm run build:frontend
npm run verify:release
```

# FINAL ARCHITECTURAL STATEMENT

Explicitly state whether these remain true:

```text
ML prediction != canonical truth
```

```text
ML is advisory
```

```text
activity_progress remains protected
```

```text
production inference is native TypeScript
```

```text
Python is offline-only
```

```text
deterministic candidate generation remains intact
```

```text
dual-threshold auto-confirm remains intact
```

# FINAL SCOPE STATEMENT

Explicitly confirm:

> Phases 31–35 were the final planned ML phases and no unplanned ML functionality was introduced.

If any criterion is genuinely unmet, report `BLOCKED` for the relevant phase rather than disguising it as complete.

Do not say “looks good”.

Provide evidence.

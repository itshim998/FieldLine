# FieldLine — PASS 6: Schedule Validation

You are implementing PASS 6 — Schedule Validation.

FieldLine repository:

itshim998/FieldLine

Local directory:

C:\Users\Sounak\Desktop\Flagship AI projects\FieldLine

PASS 0 through PASS 5 are complete.

Before coding, inspect the current repository and implement this pass on top of the existing importer + normalization architecture.

---

# CURRENT PIPELINE

The repository currently follows:

```text
CSV / XLSX
    ↓
Parser
    ↓
CanonicalScheduleImportRow
    ↓
ScheduleNormalizer
    ↓
NormalizedScheduleActivity
    ↓
ScheduleRepository.createWithActivities()
    ↓
SQLite

PASS 6 must introduce validation between normalization and persistence:

CSV / XLSX
    ↓
Parser
    ↓
Normalization
    ↓
PASS 6 VALIDATION
    ↓
atomic persistence
    ↓
SQLite

The database transaction already exists. Do not weaken it.

OBJECTIVE

Prevent invalid schedule data from entering the FieldLine project model.

Validate:

unique activity identifiers within the imported schedule;
required/critical fields;
valid normalized dates;
start <= finish;
valid percentages;
valid quantities;
duplicate activities;
schedule is not empty.

Produce useful import validation errors.

EXIT CRITERION

Invalid schedule data is rejected cleanly before any schedule/activity records are persisted.

A valid normalized schedule continues through the existing atomic persistence path unchanged.

CRITICAL PASS BOUNDARY

Do NOT implement later functionality.

Do not add:

AI validation;
schedule quality scoring;
anomaly detection;
WBS hierarchy validation;
dependency/network validation;
resource validation;
calendar validation;
advanced duplicate reconciliation;
schedule optimization;
progress logic;
matching logic.

PASS 6 is deterministic schedule validation only.

FIRST: INSPECT CURRENT IMPLEMENTATION

Inspect:

schedule-import.service.ts;
normalization layer;
NormalizedScheduleActivity;
parser/header validation;
existing AppError hierarchy;
ScheduleRepository.createWithActivities;
schedule/activity tests;
import route;
frontend importer;
existing import error UI.

Do not duplicate existing normalization.

Do not move normalization responsibilities into validation.

VALIDATION ARCHITECTURE

Create an explicit validation boundary.

Prefer something conceptually like:

backend/src/services/validation/
    schedule-validator.ts
    validation-types.ts

or another structure consistent with the repository.

The validator should operate on:

NormalizedScheduleActivity[]

and return either:

valid

or a structured collection of validation issues.

Keep the validator deterministic and testable.

Prefer pure validation functions where practical.

IMPORTANT: AGGREGATE ERRORS

Do not stop at the first invalid row.

A file containing:

2 missing names;
1 invalid quantity;
3 date-order errors;
1 duplicate ID;

should return all of those issues together.

A useful structure is conceptually:

ScheduleValidationResult

isValid
issues[]

ScheduleValidationIssue:
    code
    message
    rowNumber?
    field?
    value?

Adapt names to existing conventions.

Do not create an unnecessarily large validation framework.

VALIDATION ERROR

Create a focused application error for schedule validation if appropriate, for example:

ScheduleValidationError

It should contain the structured issues.

Use the existing AppError architecture.

Return an HTTP status appropriate for semantically invalid client-provided import data, such as:

422 Unprocessable Entity

unless the existing error conventions strongly indicate another status.

Do not expose stack traces or raw SQLite errors.

1. REQUIRED / CRITICAL FIELDS

After normalization, require:

externalId
name
plannedStart
plannedFinish

These should normally already be enforced by the parser/normalizer, but Pass 6 must treat them as final domain validation.

Reject:

empty external ID;
empty name;
missing plannedStart;
missing plannedFinish.

Do not invent missing values.

2. UNIQUE ACTIVITY IDENTIFIERS

Activity IDs must be unique within the imported schedule.

Example:

ACT-001
ACT-002
ACT-001

must fail.

Comparison should use the same deterministic identity policy already used by the importer.

Because normalization preserves identifier case/punctuation, use a clearly documented comparison policy. The current importer treats IDs case-insensitively, so preserve that behavior unless there is a concrete reason to change it.

For example:

ACT-001
act-001

should be treated as a duplicate within the same imported schedule.

VERY IMPORTANT

Do NOT enforce project-global uniqueness.

The same external activity ID may legitimately appear in different schedule versions of the same project.

The uniqueness scope is:

one imported schedule

not:

entire project
3. DUPLICATE ACTIVITIES

Detect duplicate activity records within the same import.

At minimum duplicate identity is based on:

externalId

Do not invent fuzzy duplicate detection.

Do not compare activity names/descriptions to guess duplicates.

If the same external ID occurs twice, report both relevant row locations where practical.

4. DATE VALIDATION

The normalization layer already converts supported inputs into:

YYYY-MM-DD

and rejects impossible calendar dates.

PASS 6 must now validate schedule semantics.

For every activity:

plannedStart <= plannedFinish

must be true.

Example:

start  = 2026-05-10
finish = 2026-05-01

must fail.

Do not silently swap the dates.

Do not repair invalid dates.

Report:

row;
field(s);
values;
clear reason.

Example:

Row 17: Start date 2026-05-10 is after finish date 2026-05-01.

Do NOT validate schedule dependencies or predecessor relationships in this pass.

5. PERCENTAGE VALIDATION

Validate:

baselineProgress

as a percentage in:

0 <= value <= 100

The normalizer may already convert percentage strings to numbers.

PASS 6 must enforce the domain range.

Examples:

0      → valid
50     → valid
100    → valid
-1     → invalid
101    → invalid

Do not reinterpret:

0.5

as 50.

Whatever semantic normalization produced must simply be validated against the final normalized value.

6. QUANTITY VALIDATION

If:

plannedQuantity

is not null, it must be:

finite;
numeric;
non-negative.

Examples:

100      → valid
0        → valid
1250.5   → valid
-10      → invalid
Infinity → invalid
NaN      → invalid

Do not perform unit conversion.

Do not decide whether the quantity is realistic for the activity.

That is outside this pass.

7. UNIT / QUANTITY CONSISTENCY

Do NOT introduce broad unit semantics.

At most, verify a simple structural rule:

If plannedQuantity is present, the unit may be null unless the existing domain explicitly requires one.

Do not reject:

quantity = 100
unit = null

unless the current repository/domain model already establishes that as mandatory.

Do not create unit-conversion rules.

8. EMPTY SCHEDULE

An import containing zero normalized activities must be rejected.

The existing importer already detects empty parser results, but retain this as a final schedule-level invariant.

Example error:

Schedule import contains no activities.
9. VALIDATION ORDER

The intended processing order should be:

parse
  ↓
normalize
  ↓
validate
  ↓
persist

Specifically:

parser reads source;
normalizer produces NormalizedScheduleActivity[];
validator evaluates the complete normalized schedule;
if invalid → throw validation error;
if valid → proceed to repository transaction.

No database writes should occur before validation completes.

10. REMOVE DUPLICATED VALIDATION

The current ScheduleImportService performs an explicit normalized activity-ID duplicate check.

Move that responsibility into the new schedule validator rather than keeping two competing duplicate checks.

After this pass, the architecture should be:

ScheduleImportService
    ↓
parser
    ↓
normalizer
    ↓
schedule validator
    ↓
repository

The service should orchestrate these steps, not contain the validation rules itself.

Do not duplicate rules in:

route;
repository;
database;
service.
11. VALIDATION RESULT SHOULD BE PURE

The validator must NOT:

access SQLite;
mutate database state;
call repositories;
depend on Express;
read files;
use external APIs.

It validates in-memory normalized data.

This makes validation deterministic and easy to test.

12. PERSISTENCE SAFETY

The most important requirement:

An invalid schedule must leave zero new schedule/activity persistence artifacts.

For an invalid import:

parse
→ normalize
→ validate fails
→ no repository transaction

Therefore there should be:

0 new schedules
0 new activities

The temporary upload file should still be cleaned up by the existing importer finally/cleanup path.

13. IMPORTER ERROR RESPONSE

When validation fails through the HTTP API, return a structured response that the frontend can understand.

For example:

{
  "error": "SCHEDULE_VALIDATION_ERROR",
  "message": "Schedule contains validation errors",
  "issues": [
    {
      "row": 4,
      "field": "plannedFinish",
      "code": "START_AFTER_FINISH",
      "message": "Start date 2026-05-10 is after finish date 2026-05-01"
    }
  ]
}

Adapt this to the existing AppError response format.

Do not hardcode the exact JSON shape if the existing architecture already has a standard error envelope; extend that instead.

14. FRONTEND ERROR DISPLAY

Do not redesign the importer UI.

Modify the existing import error display so validation failures show useful information.

For example:

Schedule contains 4 validation errors

Row 4 — Finish Date
Start date is after finish date.

Row 9 — Activity ID
Duplicate activity ID: ACT-004.

Row 17 — Quantity
Quantity must be non-negative.

The exact visual design is up to the existing UI conventions.

The goal is human-readable import feedback.

Do not create a full validation-review workflow yet.

15. TESTS

Create a dedicated validator test suite.

At minimum test:

Required fields
missing external ID;
missing name;
missing start;
missing finish.
Dates
equal dates → valid;
start before finish → valid;
start after finish → invalid.
Percentages
0 → valid;
50 → valid;
100 → valid;
negative → invalid;

100 → invalid.

Quantities
positive → valid;
zero → valid;
negative → invalid;
NaN/Infinity → invalid.
Duplicate IDs
unique IDs → valid;
same ID twice → invalid;
same ID with different case → invalid according to current identity policy;
same ID in separate hypothetical schedule inputs should not be treated as a global project violation.
Empty schedule
zero rows → invalid.
Aggregation

Create one fixture containing multiple independent errors and verify all issues are returned.

16. IMPORT SERVICE TESTS

Extend the importer service tests.

At minimum verify:

Valid import
parse
→ normalize
→ validate
→ persist

still succeeds.

Invalid import

For a schedule containing multiple validation errors:

importSchedule()
→ throws ScheduleValidationError

and:

schedule count remains unchanged
activity count remains unchanged
Duplicate IDs

Ensure the moved validation rule still behaves exactly as intended.

Existing valid CSV/XLSX

All existing valid imports must continue to work.

17. API TESTS

Extend schedule-router tests for:

POST /api/projects/:projectId/schedules/import

Test:

valid import;
invalid date ordering;
duplicate IDs;
invalid quantity;
invalid percentage;
multiple validation errors;
no database artifacts after rejected import;
useful error response.
18. CSV/XLSX CONSISTENCY

Pass 5 already proves equivalent CSV/XLSX representations normalize to equivalent objects.

Pass 6 should validate the normalized objects identically.

For equivalent valid source data:

CSV
 ↓
normalize
 ↓
validate
 ↓
valid

XLSX
 ↓
normalize
 ↓
validate
 ↓
valid

For equivalent invalid data, both should report equivalent validation outcomes.

19. DATABASE

Do NOT modify the migration chain for this pass.

Validation should happen before persistence.

No new database columns are required.

Do not move validation into SQLite constraints unless there is a concrete existing constraint that naturally supports it.

This pass is primarily an application-level schedule validator.

20. PASS 7 BOUNDARY

Do NOT implement manual progress reporting.

Do not add:

progress entry UI;
actual progress records;
activity progress updates;
planned-vs-actual calculations;
variance;
delay.

Those belong to later passes.

21. SELF-REVIEW

Before committing, inspect the architecture and ask:

Ordering

Is it truly:

parse
→ normalize
→ validate
→ persist
Purity

Does validation avoid SQLite/files/HTTP?

Completeness

Are all required validation rules covered?

Error quality

Can a user understand what is wrong and where?

Atomicity

Can an invalid import leave any schedule/activity records behind?

Scope

Did any Pass 7+ functionality leak in?

Regression

Do Pass 0–5 tests still pass?

Local-only architecture

Did anything external or deployment-related get introduced?

The answer must be no.

VALIDATION COMMANDS

Run:

npm test
npm run build
npm run setup
npm run setup
npm run demo:reset

Then:

npm run dev

Manually verify:

Open FieldLine.
Open a project.
Import a valid CSV.
Confirm it succeeds.
Import a valid XLSX.
Confirm it succeeds.
Import a schedule with:
duplicate activity ID;
start > finish;
negative quantity;
baseline progress > 100;
multiple errors in different rows.
Confirm the import is rejected.
Confirm the UI shows all relevant issues.
Confirm no schedule or activity records were created by the invalid import.
Confirm /api/health remains healthy.
GIT

Inspect:

git status
git diff

Confirm the changes are limited to PASS 6.

Create:

feat: implement FieldLine schedule validation

Push to:

itshim998/FieldLine
main
COMPLETION CRITERIA

PASS 6 is complete only when:

 Dedicated schedule validation layer exists.
 Validation occurs after normalization.
 Validation occurs before persistence.
 Required fields are enforced.
 Activity IDs are unique within each imported schedule.
 Duplicate activities are rejected.
 Dates are semantically ordered.
 Percentages are within 0–100.
 Quantities are finite and non-negative.
 Empty schedules are rejected.
 Multiple validation errors can be reported together.
 Invalid imports create zero schedule/activity database artifacts.
 Temporary files are still cleaned up.
 HTTP validation errors are structured.
 Frontend displays useful validation errors.
 CSV/XLSX validation behavior is equivalent.
 Existing valid imports still work.
 Pass 7+ functionality was NOT implemented.
 npm test passes.
 npm run build passes.
 setup passes.
 repeated setup passes.
 demo reset passes.
 health endpoint remains healthy.
 final diff reviewed.
 commit created.
 commit pushed.
FINAL REPORT

Report:

Validation architecture.
Validation rules implemented.
Validation error structure.
Duplicate-ID policy.
Importer integration.
Frontend validation-error behavior.
Tests added.
Invalid-import rollback/no-artifact verification.
CSV/XLSX consistency verification.
Results of all validation commands.
Manual verification results.
Any schema changes and justification.
Self-review findings.
Commit hash/message/branch/push result.

Explicitly confirm:

PASS 7 and later functionality were NOT implemented.

FINAL INSTRUCTION

Implement PASS 6 — Schedule Validation only.

The objective is:

No semantically invalid schedule should enter the FieldLine project model, and the user should receive clear actionable errors without leaving partial database state.

Stop after PASS 6.
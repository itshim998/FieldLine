# FieldLine — PASS 5: Schedule Normalization

You are implementing PASS 5 — Schedule Normalization.

FieldLine repository:

itshim998/FieldLine

Local directory:

C:\Users\Sounak\Desktop\Flagship AI projects\FieldLine

PASS 0 through PASS 4 are complete.

Before coding, inspect the current repository carefully and implement Pass 5 on top of the existing importer architecture.

---

# CURRENT STATE

PASS 4 already implements:

```text
CSV / XLSX
    ↓
file validation
    ↓
parser
    ↓
header mapping
    ↓
CanonicalScheduleImportRow
    ↓
schedule/activity persistence

The current canonical importer row contains:

externalId
name
plannedStart
plannedFinish
description
wbsCode
location
plannedQuantity
unit

The current parsers intentionally preserve much of the source representation.

PASS 5 must introduce a deterministic normalization layer before persistence.

OBJECTIVE

Create the canonical internal activity representation.

The core pipeline becomes:

Uploaded CSV/XLSX
    ↓
parser
    ↓
source-canonical rows
    ↓
NORMALIZATION
    ↓
normalized FieldLine activity representation
    ↓
repository
    ↓
SQLite
EXIT CRITERION

Equivalent schedule information from different CSV/XLSX source representations produces the same normalized internal activity format.

For example, reasonable equivalent representations of:

Activity ID
Activity name
Start date
Finish date
Quantity
Unit
whitespace
missing values

must converge to the same deterministic representation.

CRITICAL PASS BOUNDARY

Do NOT implement PASS 6.

PASS 5 is normalization, NOT schedule validation.

Do not implement:

schedule quality scoring;
impossible-date detection such as finish < start;
dependency validation;
duplicate-resolution policies;
WBS hierarchy validation;
semantic activity validation;
AI-assisted normalization;
schedule anomaly detection;
business-rule validation.

Normalization may reject a value only when it is impossible to transform it into the canonical representation required by the application.

Do not turn this into a validation engine.

FIRST: INSPECT THE CURRENT REPOSITORY

Inspect:

CanonicalScheduleImportRow;
CSV parser;
XLSX parser;
header mapper;
schedule import service;
schedule repository;
activity repository;
importer tests;
existing domain models;
existing frontend importer;
existing error handling;
package dependencies.

Do not duplicate functionality already present in Pass 4.

NORMALIZATION ARCHITECTURE

Create an explicit normalization boundary.

Prefer something conceptually like:

backend/src/services/normalization/
    schedule-normalizer.ts
    date-normalizer.ts
    number-normalizer.ts
    text-normalizer.ts
    unit-normalizer.ts

Adapt the structure to current repository conventions.

Do not create empty abstractions.

The architecture should be:

ScheduleImportService
    ↓
Parser
    ↓
CanonicalScheduleImportRow
    ↓
ScheduleNormalizer
    ├── text normalization
    ├── date normalization
    ├── number normalization
    ├── percentage normalization where applicable
    ├── unit normalization
    └── missing-value normalization
    ↓
Normalized activity inputs
    ↓
Repository
CANONICAL INTERNAL REPRESENTATION

Define a clear normalized representation.

At minimum it should have:

externalId: string
name: string
description: string | null
wbsCode: string | null
location: string | null
plannedStart: string
plannedFinish: string
plannedQuantity: number | null
unit: string | null

Use the project's existing date/TypeScript conventions.

The key requirement is that downstream persistence receives one consistent representation regardless of CSV/XLSX source formatting.

Do not keep source-specific representations in the persistence layer.

1. TEXT NORMALIZATION

Normalize textual fields deterministically.

For:

externalId
name
description
wbsCode
location
unit

apply appropriate minimal normalization:

Whitespace
trim leading/trailing whitespace;
collapse repeated internal whitespace where safe;
convert empty strings to null for nullable fields.

Example:

"  Excavation   Work  "
→
"Excavation Work"

Do not aggressively rewrite meaningful content.

IDs

For externalId:

trim whitespace;
preserve the actual identifier semantics;
do NOT arbitrarily remove punctuation;
do NOT invent IDs;
do NOT lowercase identifiers unless the existing application explicitly defines IDs as case-insensitive.

The goal is canonical formatting, not changing identifiers.

Names

Trim and collapse unnecessary whitespace.

Do not change capitalization purely for aesthetics unless there is a documented canonical rule.

2. DATE NORMALIZATION

Create a dedicated deterministic date normalizer.

All normalized schedule dates should use one representation.

Prefer:

YYYY-MM-DD

unless the existing application has a stronger established convention.

Support common unambiguous source forms, including where safely possible:

YYYY-MM-DD
YYYY/MM/DD
YYYY-MM-DDTHH:mm:ss...
Excel date values
JavaScript Date values

Also support common day-first date representations where the interpretation is deterministic.

Important:

Do not silently guess between ambiguous formats such as:

01/02/2026

unless the repository establishes an explicit rule.

Choose a deterministic policy and document it.

A good approach is:

ISO formats are unambiguous;
Excel date values are interpreted using Excel semantics;
unambiguous slash-form dates can be normalized;
genuinely ambiguous dates should produce a clear normalization error rather than silently changing the date.

Do NOT yet enforce semantic rules such as:

finish >= start

That belongs to PASS 6.

EXCEL DATE HANDLING

This is important.

CSV may provide:

2026-04-01
01/04/2026
Apr 1, 2026

while XLSX may provide an actual Excel date value.

The normalization layer must ensure that equivalent date values from CSV/XLSX ultimately become the same canonical string.

Do not leak SheetJS-specific date representations downstream.

3. NUMBER NORMALIZATION

Create a deterministic number normalizer.

At minimum support reasonable numeric representations such as:

1250
"1250"
"1,250"
"1.25E3"
" 1250 "

where the representation is unambiguous.

Normalize them to a JavaScript number.

Examples:

"1,250" → 1250
"1250.00" → 1250
"1.25E3" → 1250

Do not silently reinterpret ambiguous locale-specific decimal formats such as:

1.234,56

unless there is a deterministic rule.

Do NOT validate whether a quantity is sensible for the project. That belongs to later validation/domain logic.

4. PERCENTAGE NORMALIZATION

The current schedule activity representation should remain compatible with:

baselineProgress

even if Pass 4 currently defaults it to zero.

Where a source percentage is actually present and the importer supports it, normalize equivalent forms such as:

0.5
50
"50%"
" 50 % "

according to one explicit semantic rule.

The important distinction is:

50%
= 50 percent

whereas:

0.5

must NOT be automatically assumed to mean 50% unless the source field semantics establish that convention.

Do not invent percentage data when the schedule does not provide it.

Do not implement progress/actual-progress logic.

5. UNIT NORMALIZATION

Normalize common equivalent unit representations to a deterministic canonical form.

Examples where justified:

"kg" → "kg"
"KGS" → "kg"
"meter" → "m"
"metre" → "m"
"meters" → "m"
"MT" → "t"         only if the semantic meaning is unambiguous

Keep the unit normalization dictionary small and explicit.

Do not build a general-purpose units/conversion engine.

Most importantly:

Normalize unit labels, but do NOT convert quantities between units.

For example:

1000 kg

must not automatically become:

1 t

in Pass 5.

Unit conversion is outside this pass.

Unknown units should be preserved in a normalized textual form rather than invented or silently discarded.

6. MISSING VALUE NORMALIZATION

Establish one representation for missing nullable values.

For example, normalize:

""
" "
null
undefined

to:

null

for nullable fields.

Do not turn missing required fields into fabricated values.

Do not use:

"N/A"
"unknown"
"-"

as fake data.

For required fields such as:

externalId
name
plannedStart
plannedFinish

the normalizer should clearly report when no canonical value can be produced.

7. NORMALIZER MUST BE DETERMINISTIC

The same input must always produce the same normalized output.

Do NOT:

use an LLM;
use current time;
depend on locale;
use random IDs;
use browser state;
inspect external services.

Normalization must be pure/repeatable.

Prefer pure functions where possible.

8. IMPORTER INTEGRATION

Modify ScheduleImportService so the flow becomes:

parse
 ↓
canonical rows
 ↓
normalize
 ↓
normalized rows
 ↓
duplicate checks where already defined
 ↓
repository

The repository should receive the normalized representation.

Do not put normalization inside:

SQLite;
repository SQL;
React;
HTTP routes.
9. IMPORTANT DATA-PRESERVATION RULE

Normalization must never silently destroy meaningful source information.

Examples:

Do NOT:

"ACT-001/A"
→
"ACT001A"

Do NOT:

"Pump Station 01"
→
"pump station 1"

Do NOT invent WBS codes.

Do NOT invent quantities.

Do NOT invent units.

Do NOT invent dates.

Normalization should canonicalize representation, not reinterpret project information.

10. ERROR MODEL

Use the existing AppError architecture.

Introduce a focused normalization error if useful, but do not create a new elaborate error hierarchy.

A normalization failure should clearly identify:

field;
source value;
reason;
row where appropriate.

Example:

Row 17: Could not normalize Start Date '31/31/2026'

Do not expose parser stack traces.

11. TESTS

Create dedicated normalization tests.

At minimum test:

Text
"  Activity   A  " → "Activity A"
" " → null
IDs

Verify whitespace normalization without destructive rewriting.

Dates

Test equivalent forms:

2026-04-01
2026/04/01
Excel date equivalent

where supported.

Test ambiguous dates according to the documented policy.

Test invalid/unparseable values.

Numbers

Test:

1250
"1250"
"1,250"
"1250.00"
"1.25E3"
Percentages

Test supported representations explicitly.

Units

Test supported aliases.

Missing values

Test empty/whitespace/null behavior.

Canonical equivalence

Most importantly, provide cases where different source representations produce exactly the same normalized object.

For example:

CSV representation
        ↓
Normalizer
        ↓
Canonical Activity A

XLSX representation
        ↓
Normalizer
        ↓
Canonical Activity A

The outputs should compare equal.

12. IMPORTER REGRESSION TESTS

Extend existing importer tests so that:

CSV import receives normalized values;
XLSX import receives normalized values;
CSV and XLSX fixtures containing equivalent data produce equivalent activity representations;
persistence stores the normalized values.

Do not remove existing parser tests.

13. FRONTEND

Pass 5 does NOT need a major UI redesign.

The existing activity table can continue to display the normalized values.

Optionally show a small import summary such as:

Normalized successfully

but do not build a normalization review UI yet.

Pass 6 will eventually deal with validation and richer feedback.

14. DATABASE

Do NOT modify the existing migration chain unless absolutely necessary.

The existing:

schedules
activities

schema should be sufficient for normalization.

If a schema change appears necessary, do not silently add one just to accommodate the implementation.

Explain why it is needed in the final report.

15. PASS 6 BOUNDARY CHECK

Before committing, explicitly verify that you did NOT implement:

finish-before-start validation;
date-range validation;
WBS hierarchy validation;
semantic schedule validation;
advanced duplicate resolution;
schedule quality scoring;
anomaly detection.

A value becoming canonical does NOT mean it is semantically valid.

Example:

plannedStart = 2026-05-10
plannedFinish = 2026-05-01

Pass 5 may represent both as canonical dates.

PASS 6 decides that the relationship is invalid.

VALIDATION

Run:

npm test
npm run build
npm run setup
npm run setup
npm run demo:reset

Then run:

npm run dev

Manually verify:

Create/open a project.
Import a CSV containing mixed formatting.
Verify normalized values in the activity table.
Import an equivalent XLSX file.
Verify equivalent normalized values.
Verify missing optional values become cleanly null/empty in the UI.
Verify an invalid/unparseable normalization input produces a clear error.
Verify /api/health remains healthy.
SELF-REVIEW

Before committing, inspect the final diff.

Ask:

Architecture

Does the pipeline now remain:

parser
→ canonical source row
→ normalizer
→ persistence
Determinism

Would the same source data always produce the same normalized object?

Data preservation

Did normalization avoid destructive rewriting?

Cross-format consistency

Do CSV and XLSX converge to the same internal structure?

Scope

Did anything from PASS 6 or later leak in?

Regression

Does the complete Pass 3 project lifecycle and Pass 4 importer still work?

Local-only constraint

Did you introduce any external deployment/storage/service dependency?

The answer must be no.

GIT

Inspect:

git status
git diff

Confirm only PASS 5 work is included.

Create:

feat: implement FieldLine schedule normalization

Push to:

itshim998/FieldLine
main
COMPLETION CRITERIA

PASS 5 is complete only when:

 A dedicated normalization layer exists.
 Text is normalized deterministically.
 whitespace is normalized.
 missing nullable values become null consistently.
 dates converge to one canonical representation.
 Excel/CSV equivalent dates converge.
 numbers converge to numeric values.
 percentages are normalized where explicitly supported.
 units are normalized through an explicit dictionary.
 no unit conversion is performed.
 IDs are normalized without destructive rewriting.
 CSV and XLSX produce equivalent normalized activities.
 normalized values are what reach SQLite.
 normalization is deterministic and testable.
 normalization failures are clear.
 existing importer functionality remains intact.
 Pass 6 validation logic was NOT implemented.
 npm test passes.
 npm run build passes.
 npm run setup passes.
 repeated setup passes.
 npm run demo:reset passes.
 /api/health remains healthy.
 final diff reviewed.
 commit created.
 commit pushed.
FINAL REPORT

Report:

Normalization architecture.
Canonical internal representation.
Date normalization rules.
Number/percentage normalization rules.
Unit normalization dictionary.
Missing-value behavior.
Files changed.
Tests added.
CSV/XLSX equivalence tests.
Validation command results.
Manual verification result.
Any schema changes and justification.
Self-review findings.
Commit hash/message/branch/push result.

Explicitly confirm:

PASS 6 and later functionality were NOT implemented.

FINAL INSTRUCTION

Implement PASS 5 — Schedule Normalization only.

The objective is:

Different schedule-source representations should converge into one deterministic FieldLine activity representation before persistence.

Do not move into schedule validation, anomaly detection, AI, progress, or matching.

Stop after PASS 5.
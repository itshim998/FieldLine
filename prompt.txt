````text
# FieldLine — PASS 2 Final Corrective Fix

One persistence issue remains.

Do ONLY the fix below. Do not implement PASS 3 or unrelated features.

## Problem

The corrected cross-project integrity constraints/triggers currently live in migration `0002_core_domain_schema`.

That is insufficient for existing databases where `0002` has already been recorded, because the migration engine will skip `0002`.

Therefore we need a new migration that upgrades existing `0002` databases to the corrected schema.

## Required fix

Create:

```text
backend/src/database/migrations/0004_upgrade_cross_project_integrity.ts
````

Register it after `0003`.

Migration chain must now be:

```text
0001
0002
0003
0004
```

`0004` must safely upgrade an existing database that already has `0001` + `0002`.

It must add the corrected cross-project integrity enforcement for:

* activity → schedule
* evidence → progress_update
* activity_match → progress_update
* activity_match → activity
* activity_match → evidence
* activity_progress → activity
* activity_progress → progress_update

Preserve the intended delete semantics:

* evidence → progress_update: `ON DELETE SET NULL`
* activity_match → evidence: `ON DELETE SET NULL`
* activity_progress → progress_update: `ON DELETE SET NULL`
* existing cascade relationships should remain cascades.

Do NOT rewrite migration history.

Do NOT modify existing migration names.

Because SQLite cannot simply alter all existing foreign-key definitions in place, use the safest SQLite-compatible migration strategy required to rebuild/replace the affected tables while preserving existing data.

The migration must be transactional.

## Mandatory migration test

Add a migration test that represents a REAL existing database:

1. Create a database with the old `0001` + `0002` schema.
2. Insert representative existing data across projects, schedules, activities, progress updates, evidence, activity matches, and activity progress.
3. Mark `0001`, `0002`, and `0003` as already applied.
4. Run the migration engine.
5. Verify only `0004` is newly applied.
6. Verify all existing valid data remains intact.
7. Verify cross-project relationships are now rejected.
8. Verify valid same-project relationships still work.
9. Verify deleting a progress update:

   * preserves evidence;
   * sets `evidence.progress_update_id` to NULL;
   * sets nullable `activity_progress.progress_update_id` to NULL;
   * preserves standalone evidence.
10. Run migrations again and verify nothing is reapplied.

Also retain the existing fresh-database migration tests.

## Validation

Run:

```bash
npm test
npm run build
npm run setup
npm run setup
npm run demo:reset
```

Then verify:

```text
GET /api/health → HTTP 200
schema_version → 0.2.0
```

Inspect the final schema and migration chain manually before committing.

## Git

Commit:

```text
fix: finalize FieldLine Pass 2 migration compatibility
```

Push to:

```text
itshim998/FieldLine
main
```

Final report must include:

* migration chain;
* existing-database upgrade test result;
* data-preservation result;
* cross-project integrity result;
* SET NULL behavior result;
* full test result;
* build/setup/reset results;
* health result;
* commit hash.

Explicitly confirm:

> PASS 3 was NOT implemented.

```
```

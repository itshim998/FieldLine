# FieldLine Pass 25 — Final Integration, Release Hardening & Presentation Polish

Repository:

`itshim998/FieldLine`

Branch:

`main`

## Canonical baseline

The current canonical baseline is Pass 24:

`c31895deced67e47a883f9d2a6ea0d9ea20dfece`

Commit:

`feat: add FieldLine golden demo environment`

Verify the actual current HEAD before making changes. Do not assume the SHA above is still the latest commit.

Pass 24 is approved and already provides:

* deterministic Golden Demo project: `Refinery Expansion — Unit 4`
* project code: `REFINERY-U4`
* 30 activities
* six EPC work areas
* multi-date historical evidence
* physical evidence fixtures
* deterministic canonical progress
* review-state examples
* delayed / at-risk / completed / on-track distribution
* approaching milestones
* `npm run demo:reset`
* `npm run demo:verify`
* machine-checkable demo invariants
* offline MockAI assistant support
* automatic selection of the sole golden project on fresh startup

Pass 23 is also approved and remains the canonical regression suite.

Pass 22 remains deliberately BENCHED.

---

# PASS 25 OBJECTIVE

Do NOT build another major product feature.

The objective is:

> Turn the current working FieldLine system into a coherent, release-ready, SIH-presentation-ready application.

Pass 24 established the golden environment.

Pass 25 must make that environment:

* polished
* coherent
* recoverable
* documented
* responsive
* operationally reliable

Think:

```text
Pass 23 = prove the system
Pass 24 = create the demo state
Pass 25 = make the whole thing presentation-ready
```

---

# FIRST TASK — AUDIT THE ACTUAL REPOSITORY

Before editing:

1. Inspect current `main` HEAD.
2. Inspect frontend architecture.
3. Inspect current dashboard components.
4. Inspect Activity Detail components.
5. Inspect assistant UI and data model.
6. Inspect evidence/upload UI.
7. Inspect review UI.
8. Inspect all loading/error/empty-state handling.
9. Inspect `scripts/setup.ts`.
10. Inspect `scripts/demo-reset.ts`.
11. Inspect `scripts/demo-verify.ts`.
12. Inspect `demo/golden-demo-manifest.ts`.
13. Inspect `demo/golden-demo-seeder.ts`.
14. Inspect `package.json`.
15. Inspect `docs/`.
16. Search for:

* debug logs
* TODO/FIXME
* stale pass numbers
* dead imports
* dead components
* temporary files
* hardcoded demo values
* absolute local paths

Do not perform a large rewrite.

---

# SECTION 1 — FINAL DEMO JOURNEY

Make this journey work smoothly:

```text
Open FieldLine
↓
Golden Project automatically selected
↓
Dashboard
↓
Identify delayed / at-risk activity
↓
Open Activity Detail
↓
Inspect execution history
↓
Inspect evidence/review context
↓
Return to dashboard
↓
Open Assistant
↓
Ask grounded question
↓
Show verified facts
```

Also ensure this core workflow remains demonstrable:

```text
Schedule
↓
Field Report
↓
Matching
↓
Review
↓
Canonical Progress
↓
Variance
```

Do not add new functionality solely for the demo.

---

# SECTION 2 — LOADING STATES

Audit every asynchronous surface.

Ensure visible feedback exists for:

* initial application load
* project list loading
* project selection
* dashboard loading
* activity-detail loading
* schedule loading
* evidence loading
* evidence upload
* evidence processing
* intelligence loading
* assistant loading
* schedule import
* manual report submission
* review mutations

Avoid blank UI while requests are active.

Reuse existing styling/components where possible.

Do not build an unnecessarily complex skeleton framework.

---

# SECTION 3 — EMPTY STATES

Audit:

* no projects
* no schedules
* no activities
* no evidence
* no reports
* no pending reviews
* no intelligence entries
* no recent changes
* no assistant response

Each empty state should clearly explain what is missing and what action is relevant.

Do not make empty states interfere with the golden demo first-load experience.

---

# SECTION 4 — ERROR STATES

Audit network/API/backend failure handling.

Important cases:

* backend unavailable
* invalid project
* invalid activity
* upload failure
* processing failure
* assistant failure
* invalid response
* malformed input

The UI should:

* show a useful message
* avoid raw stack traces
* avoid misleading stale content
* provide sensible recovery where appropriate

Do not unnecessarily change API error contracts.

---

# SECTION 5 — DASHBOARD POLISH

Use the EXISTING dashboard architecture.

Do not create another dashboard engine.

The golden project deliberately contains:

* 4 delayed
* 4 at-risk
* 4 completed
* 18 on-track/ahead
* 4 approaching milestones

Make those distinctions obvious.

Refine:

* Project Health
* Activity Status
* Attention
* Milestones
* Recent Updates

Make important information scannable within seconds.

Do not hardcode any golden values into frontend components.

---

# SECTION 6 — ACTIVITY DETAIL POLISH

The current Activity Detail architecture already exposes:

* identity
* current state
* timeline
* progress sources
* review context
* evidence

Make the page tell one coherent execution story.

For rich golden activities such as:

* `ACT-B02`
* `ACT-C01`
* `ACT-A02`

the user should immediately understand:

```text
planned
→ observed
→ matched
→ reviewed
→ actual progress
→ risk
→ source evidence
```

Do not add new business calculations.

Use current backend DTOs.

---

# SECTION 7 — ASSISTANT UX

Use the current Assistant API contract.

The backend exposes:

* intent
* resolved activity
* ambiguous candidates
* answer
* claims
* factRefs
* grounded
* status
* asOfDate
* verifiedFacts

Use these existing fields to make the UI trustworthy.

The user should visually understand:

```text
AI-generated wording
        vs
Verified FieldLine fact
```

Improve:

* suggested questions
* loading state
* response formatting
* fact/claim display
* grounded indicator
* activity resolution
* failure state
* empty state
* as-of-date context

Do not weaken grounding.

Do not add a parallel answer path.

---

# SECTION 8 — EVIDENCE UX

Make evidence traceability clear.

Ensure:

* filename is readable
* evidence type is clear
* originating report is understandable
* Activity Detail shows the relevant source
* no absolute filesystem paths are shown
* unavailable evidence gets a clean error state

Do not add a new document viewer unless the current architecture already supports it without scope expansion.

---

# SECTION 9 — REVIEW UX

Preserve current review semantics:

```text
suggested
↓
human action
↓
confirmed / rejected / resolved
```

Make the canonical-truth boundary visually clear.

Do not automatically confirm matches merely to improve the demo.

Do not introduce new review semantics.

---

# SECTION 10 — PROCESSING UX

The existing evidence-processing state should feel complete:

```text
uploaded
→ queued
→ processing
→ completed
```

or:

```text
uploaded
→ queued
→ failed
```

Add visible progress/status feedback where appropriate using the current job architecture.

Do not introduce a new job system.

---

# SECTION 11 — RESPONSIVE UI

Audit at:

* 1440px
* 1024px
* 768px
* 390–430px

Prioritize:

* dashboard
* Activity Detail
* project selector
* review
* evidence
* assistant

Fix:

* horizontal overflow
* clipped content
* broken grids
* overlapping buttons
* unreadable tables
* oversized cards
* inaccessible controls

Do not redesign the whole visual language.

---

# SECTION 12 — ENVIRONMENT HARDENING

The current environment schema already supports:

```text
AI_PROVIDER=mock
AI_PROVIDER=gemini
```

with Gemini requiring `GEMINI_API_KEY`.

Verify:

### Offline

Default/local MockAI works without credentials.

### Gemini

Missing API key fails clearly.

### Invalid configuration

Produces a clear startup error.

### Logging

Never expose secrets.

Do not redesign the environment system unless the audit finds a real defect.

---

# SECTION 13 — CLEAN INSTALLATION

Verify the documented sequence from a clean runtime:

```bash
npm install
npm run setup
npm run demo:reset
npm run demo:verify
npm run dev
```

The system must not depend on:

* developer-specific absolute paths
* pre-existing database state
* old uploads
* hidden files
* undocumented API keys

---

# SECTION 14 — RESET RELIABILITY

The current Pass 24 reset pipeline is:

```text
delete runtime DB
↓
delete uploads
↓
recreate DB
↓
migrations
↓
golden seed
↓
verification
```

Preserve this.

Verify repeated:

```bash
npm run demo:reset
npm run demo:reset
npm run demo:reset
```

produces the same logical golden state.

No duplicates should accumulate.

---

# SECTION 15 — DEMO VERIFICATION

Keep `npm run demo:verify` focused on demo integrity.

It must remain capable of detecting:

* wrong golden project
* wrong activity count
* wrong risk counts
* wrong milestone state
* missing rich histories
* missing evidence
* missing physical evidence files
* dashboard drift
* assistant grounding drift
* duplicate/unexpected golden state

Do not turn it into a replacement for the complete Vitest suite.

---

# SECTION 16 — RELEASE VERIFICATION COMMAND

Add:

```bash
npm run verify:release
```

It should orchestrate:

```text
build
+
test
+
demo reset
+
demo verify
```

Use the repository's existing commands.

Avoid recursive npm-script loops.

Do not start long-running dev servers from this command.

The purpose is:

> One command determines whether the repository is ready for presentation.

---

# SECTION 17 — DOCUMENTATION

Create:

## `docs/DEMO.md`

Include:

* SIH demo purpose
* golden project
* exact setup commands
* demo reset
* demo verification
* recommended dashboard walkthrough
* recommended Activity Detail walkthrough
* recommended review demonstration
* recommended assistant questions
* recovery instructions

## `docs/AI-PIPELINE.md`

Document:

```text
Field Evidence
↓
Normalization
↓
AI Structured Extraction
↓
Zod Contract Validation
↓
Activity Matching
↓
Confidence / Review Policy
↓
Human Confirmation
↓
Canonical Progress
↓
Deterministic Intelligence
↓
Grounded Assistant
```

Explicitly explain:

* what AI does
* what AI does not do
* why AI is not authoritative
* MockAI vs Gemini
* structured contracts
* human-review boundary
* grounding

## `docs/ARCHITECTURE.md`

Audit and update the existing architecture document.

Do not replace it wholesale.

Bring it into alignment with the ACTUAL Pass 24 codebase, including:

* demo environment
* demo verification
* Activity Detail
* current AI provider abstraction
* worker pipeline
* current frontend composition

---

# SECTION 18 — FINAL ARCHITECTURE DIAGRAM

Ensure documentation includes an accurate high-level architecture such as:

```text
                      Local Browser
                           ↓
                  FieldLine React UI
                           ↓
                  Node / Express API
                           ↓
        ┌──────────────────┼──────────────────┐
        ↓                  ↓                  ↓
 Domain Services       AI Layer          Job Worker
        ↓                  ↓                  ↓
        │            Mock / Gemini            │
        └──────────────────┼──────────────────┘
                           ↓
                        SQLite
                       /      \
                      /        \
               Project State   Evidence Files
```

And the truth pipeline:

```text
Schedule
↓
Activities
↓
Field Evidence
↓
AI Extraction
↓
Activity Matching
↓
Human Review
↓
Canonical ActivityProgress
↓
Snapshot / Variance / Risk
↓
Project Intelligence
├── Dashboard
├── Activity Detail
└── Grounded Assistant
```

Only document behavior that actually exists.

---

# SECTION 19 — CODE HYGIENE

Audit and clean:

* dead imports
* unused variables
* stale comments
* TODO/FIXME that no longer apply
* debug logging
* temporary artifacts
* old pass-number text
* dead components
* unused dependencies
* inconsistent naming

Do NOT perform a broad refactor.

Remove debris; preserve working architecture.

---

# SECTION 20 — MOCKAI BOUNDARY

Pass 24 expanded `MockAIProvider` so the offline assistant can interpret manager questions.

Keep the abstraction:

```text
AIService
    ↓
AIProvider
    ├── MockAIProvider
    └── GeminiAIProvider
```

Do not move demo-specific behavior into:

* AssistantService
* domain services
* repositories
* controllers

The domain must continue to operate independently of whether MockAI or Gemini is used.

---

# SECTION 21 — NO PASS 22

Do NOT implement voice input.

Do NOT add:

* microphone capture
* speech recognition
* transcription
* audio APIs
* voice UI

Pass 22 remains benched.

---

# SECTION 22 — NO DEMO-ONLY BUSINESS LOGIC

Absolutely do NOT introduce:

```ts
if (demoMode) { ... }
```

to fake business behavior.

Do NOT hardcode:

* dashboard numbers
* risk classifications
* assistant answers
* progress
* canonical truth

The golden demo must work because it is real valid FieldLine data.

---

# SECTION 23 — VALIDATION

Run:

```bash
npm test
npm run build
npm run demo:reset
npm run demo:verify
npm run verify:release
```

All must pass.

Then manually smoke-test:

```text
dashboard
→ delayed activity
→ Activity Detail
→ evidence
→ back
→ assistant
→ grounded question
→ review/pending item
```

---

# DEFINITION OF DONE

Pass 25 is complete when:

1. Existing functionality remains intact.
2. Full test suite passes.
3. Production build passes.
4. Golden demo reset passes.
5. Golden demo verification passes.
6. Release verification command passes.
7. Fresh installation works.
8. Dashboard is presentation-ready.
9. Activity Detail is presentation-ready.
10. Assistant UX clearly communicates grounding.
11. Evidence/review flows are understandable.
12. Loading/error/empty states are complete.
13. Responsive behavior is acceptable.
14. No obvious debug/dead-code debris remains.
15. `docs/DEMO.md` exists.
16. `docs/AI-PIPELINE.md` exists.
17. `docs/ARCHITECTURE.md` reflects the actual current architecture.
18. No voice input is added.
19. No major feature is added.
20. No demo-only business logic is added.

---

# COMMIT

Use:

`feat: finalize FieldLine integration and presentation polish`

# FieldLine Pass 23 — Critical Regression & Evaluation Suite

You are working on the FieldLine repository:

`itshim998/FieldLine`

Canonical branch:

`main`

## Current repository baseline

Before making any changes, inspect the ACTUAL current repository state and current HEAD.

The current known HEAD is:

`a3d056e7c63c06c199369df35cc62ef624bfd4aa`

Commit:

`feat: implement FieldLine activity detail view`

Do NOT assume this is still the latest commit. Verify first.

FieldLine already contains a substantial Vitest-based test suite with:

* unit tests
* repository tests
* service tests
* router/API tests
* schedule validation tests
* document extractor tests
* document ingestion tests
* job/worker tests
* idempotency tests
* transactional failure tests
* canonical truth protection tests
* assistant grounding/security tests
* Project Intelligence E2E tests
* fixtures
* golden extraction fixtures
* CSV/XLSX equivalence tests

Do NOT rebuild the test system from scratch.

---

# PASS 23 OBJECTIVE

Create a coherent, deterministic **critical-workflow evaluation suite** that proves the entire FieldLine project-truth pipeline works together.

The key question this pass must answer is:

> Can a clean local FieldLine instance take a schedule and field report, process the report through the existing ingestion/matching/review pipeline, create canonical progress safely, calculate deterministic intelligence, expose that state through the dashboard, and answer a grounded assistant question?

This pass is primarily about **system-level regression**, not adding arbitrary numbers of isolated unit tests.

---

# CORE ARCHITECTURE TO PRESERVE

FieldLine's existing architecture is authoritative:

```text
Raw Evidence
→ ingestion
→ extracted structured field facts
→ Activity Matching
→ confidence/review policy
→ human/system confirmation
→ canonical ActivityProgress
→ Progress Snapshot
→ Risk Classification
→ Project Intelligence
→ Dashboard / Activity Detail
→ grounded Assistant
```

Preserve all existing architectural invariants:

1. Project isolation
2. AI is not the source of truth
3. Only confirmed matches are eligible for canonical progress
4. Review history remains immutable
5. Review mutation + mandatory audit event remain atomic
6. Failed ingestion leaves no partial canonical state
7. Repeated processing remains idempotent
8. Evidence remains content-addressably deduplicated
9. ActivityProgress remains canonical historical progress
10. Historical views use canonical `asOfDate`
11. `completedToday` uses actual completion semantics
12. Dashboard remains a presentation/aggregation layer
13. Activity Detail remains read-only composition
14. Assistant claims remain field-verifiable
15. Assistant is read-only
16. No LLM-generated SQL
17. Uncertainty remains visible
18. Server filesystem paths never leak to clients
19. Queries remain bounded
20. Deterministic domain calculations remain authoritative

---

# FIRST TASK — REPOSITORY AUDIT

Before editing anything:

1. Inspect the latest `main` HEAD.
2. Inspect the existing test directory.
3. Identify current test helpers/fixtures.
4. Identify the current app composition / dependency injection pattern.
5. Identify how the processing worker is instantiated.
6. Identify the current schedule import route/service.
7. Identify the current evidence upload route/service.
8. Identify the current document-processing job route/service/worker.
9. Identify the current matching confirmation path.
10. Identify the current ProgressService path.
11. Identify the current snapshot/intelligence/dashboard path.
12. Identify the current AssistantService path.
13. Identify existing tests that already cover each layer.

Do not duplicate tests blindly.

---

# NEW FLAGSHIP TEST

Create one authoritative system-level regression test, conceptually:

`backend/tests/critical_workflow_e2e.test.ts`

You may choose a better filename if repository conventions suggest one, but keep the intent obvious.

This must be an **application/backend E2E test**, using the current Node/Express/Vitest/Supertest architecture.

Do NOT add Playwright or Cypress.

Use in-memory SQLite for isolation.

Use `MockAIProvider` or an equivalent deterministic provider.

Do NOT make network requests to Gemini or any external API.

---

# FLAGSHIP WORKFLOW

The primary test must cover:

```text
clean test DB
↓
create project
↓
import schedule
↓
verify activities
↓
upload field evidence
↓
create/process document job
↓
worker processes job
↓
document extraction
↓
AI field-fact extraction
↓
activity matching
↓
review/confirmation
↓
ProgressService
↓
ActivityProgress
↓
ProgressSnapshotService
↓
RiskClassification
↓
ProjectIntelligenceService
↓
dashboard endpoint
↓
assistant endpoint
```

Use the EXISTING production services and repositories.

Do not mock away the business logic.

Mock only external/non-deterministic AI behavior where required.

---

# DETERMINISTIC GOLDEN FIXTURE

Create a reusable fixture for a small realistic construction project.

The fixture should include approximately 4–6 activities.

Cover:

* one on-track activity
* one delayed activity
* one at-risk activity
* one activity that can be completed from the report
* one ambiguous/non-matching activity
* optionally one milestone

The field report fixture must include:

* explicit report date
* a clearly matchable activity
* a deterministic progress percentage
* a completed activity or completion signal
* an ambiguous observation

Also define expected outputs for the fixture:

* normalized schedule activity
* extracted field fact
* expected matching result
* expected confidence/review state
* expected canonical ActivityProgress
* expected snapshot values
* expected variance
* expected risk classification
* expected intelligence result
* expected dashboard values
* expected grounded assistant facts

Keep generated IDs/timestamps out of the golden comparison unless they are normalized.

Reuse the existing golden-fixture conventions already present in the repository.

---

# HAPPY-PATH ASSERTIONS

The flagship test must assert meaningful values, not just HTTP 200.

## Schedule

Verify:

* correct number of activities
* correct normalized names/IDs
* correct dates/quantities where relevant

Do not duplicate all schedule-validator test coverage.

## Evidence

Verify:

* correct project association
* evidence persisted
* no client-visible filesystem path leakage

## Job

Verify:

* job created
* processing occurs
* job reaches the correct final state
* result payload is bounded/sanitized

## Extraction

Verify:

* deterministic MockAI output is accepted
* extracted facts are structured
* no fabricated activity IDs are introduced by the application

## Matching

Verify:

* expected activity receives expected candidate
* confidence tier/status are correct
* ambiguous fact does not become canonical

## Review

Confirm the intended match using the existing review workflow.

Verify:

* match becomes confirmed
* reviewer metadata exists
* corresponding audit event exists
* the state transition remains atomic

## Canonical Progress

Use the EXISTING ProgressService.

Verify:

* confirmed match creates ActivityProgress
* expected progress percentage is stored
* expected observation/as-of date is preserved
* rejected/unresolved facts do not create canonical progress

## Snapshot / Variance / Risk

Query the existing canonical services.

Assert exact expected values.

Do NOT recreate production formulas inside the test.

## Intelligence

Call the real ProjectIntelligenceService or endpoint.

Assert the expected entries in:

* delayed
* atRisk
* completedToday
* behindSchedule
* approachingMilestones
* staleActivities
* recentChanges

## Dashboard

Call:

`GET /api/projects/:projectId/dashboard`

Assert the displayed aggregate values match the canonical snapshot/intelligence state.

The test must detect disagreement between dashboard aggregation and domain truth.

## Assistant

Call:

`POST /api/projects/:projectId/assistant/query`

Use a deterministic question such as:

`What is delayed?`

Verify:

* correct intent
* correct verified fact reference
* grounded result
* authoritative value
* no invented project data

The assistant should be connected to the REAL intelligence state created by the workflow.

---

# FAILURE EVALUATION

Add focused high-value failure scenarios.

Do not duplicate existing tests unnecessarily.

## 1. Malformed schedule

Verify:

* request rejected
* no partial schedule/activity truth
* existing records remain unchanged

Prefer existing invalid schedule fixtures.

## 2. Malformed document

Verify:

* processing fails
* job becomes failed
* no partial ProgressUpdate
* no new ActivityProgress

## 3. AI extraction failure

Replace only the AI dependency with a deterministic failing provider.

Verify:

* processing fails
* no ProgressUpdate
* no Match
* no ActivityProgress
* existing canonical state remains unchanged

## 4. Missing activity

Field report refers to an activity not present in the schedule.

Verify:

* no canonical progress
* safe unresolved/not-found behavior

## 5. Ambiguous activity

Create two plausible candidates.

Verify:

* match remains suggested/unresolved according to the existing policy
* no automatic canonical truth

## 6. Rejected match

Reject an otherwise plausible match.

Verify:

* no ActivityProgress is created

## 7. Duplicate evidence

Upload/process the same raw bytes twice within the same project.

Verify:

* content-addressable deduplication remains intact
* no duplicated canonical truth
* processing idempotency remains intact

## 8. Contradictory report

Create a second report containing conflicting progress information.

Do NOT invent a new conflict-resolution policy.

Instead, test the repository's current actual semantics and verify that contradictory input cannot silently corrupt canonical state.

## 9. Invalid date

Use malformed/invalid report-date information.

Verify deterministic validation/fallback behavior consistent with existing implementation.

Never use the current wall clock to create arbitrary project truth.

## 10. Missing quantity / missing percentage

Verify the application's current behavior for incomplete facts.

Do not fabricate a number just to make the test pass.

## 11. Malformed assistant claim

Configure MockAI so it returns:

* a valid factRef
* a valid-looking claim
* but an incorrect authoritative value

Verify the existing field-level grounding protection rejects the claim.

---

# CROSS-PROJECT ISOLATION

The flagship evaluation should also create Project A and Project B.

Populate each with distinct data.

Verify that Project A's:

* evidence
* processing
* matches
* progress
* intelligence
* dashboard
* assistant

never expose Project B data.

Also verify the inverse.

Do not rely only on repository-level isolation tests; this is a whole-workflow regression.

---

# DETERMINISM

Run the same deterministic fixture twice.

Compare the resulting logical outputs:

* extracted fields
* match results
* canonical progress
* snapshot
* intelligence
* dashboard facts
* assistant verified facts

Normalize generated:

* UUIDs
* creation timestamps
* other non-semantic metadata

The two logical results must be equivalent.

---

# TEST COMMAND

The repository currently has Vitest configured for backend tests.

Keep:

`npm test`

as the complete regression suite.

Add:

`npm run test:evaluation`

for the critical evaluation test.

Optionally add:

`npm run test:coverage`

only if cleanly compatible with the current setup.

Do NOT introduce arbitrary coverage thresholds simply to produce a numeric target.

---

# FILES / AREAS LIKELY TO CHANGE

Inspect before editing. Likely areas include:

* `backend/tests/`
* `backend/tests/fixtures/`
* `package.json`
* possibly a new test helper under `backend/tests/`
* possibly test-only dependency-composition helpers if the current architecture needs one

Do not modify production services unless absolutely necessary for testability.

If dependency injection prevents the flagship workflow from being composed with `MockAIProvider`, solve that minimally and architecturally.

Do NOT add production behavior merely for testing.

---

# REGRESSION REQUIREMENTS

After implementation run:

```bash
npm test
npm run build
npm run test:evaluation
```

All must pass.

Do not weaken existing tests.

Do not remove existing tests.

Do not change existing expected project semantics merely to satisfy the new evaluation suite.

If the new system-level test exposes a genuine contradiction in the current implementation, STOP and report the inconsistency rather than silently changing production semantics.

---

# SCOPE GUARD

This is a TEST/EVALUATION pass.

Do NOT implement:

* speech-to-text
* new AI providers
* new dashboard features
* new assistant capabilities
* new persistence concepts
* new project-truth semantics
* new conflict-resolution policy
* Playwright/Cypress browser testing
* cloud test infrastructure
* CI/CD redesign
* performance/load testing
* production observability

Keep Pass 23 focused on proving the current system.

---

# DEFINITION OF DONE

Pass 23 is complete when all of the following are true:

1. The complete existing regression suite passes.
2. The build passes.
3. A dedicated critical evaluation command exists and passes.
4. A deterministic end-to-end test proves:

```text
schedule
→ evidence
→ job
→ ingestion
→ extraction
→ matching
→ review
→ canonical progress
→ snapshot
→ variance/risk
→ intelligence
→ dashboard
→ assistant
```

5. Failure scenarios prove malformed or uncertain input cannot silently become canonical truth.
6. Cross-project isolation is proven across the complete workflow.
7. Assistant field-level grounding is exercised against real generated intelligence.
8. Repeated identical fixtures produce equivalent logical outputs.
9. No existing approved invariant is weakened.
10. No duplicate business engine is introduced.

---

# COMMIT

Use:

`test: add FieldLine critical workflow evaluation suite`
